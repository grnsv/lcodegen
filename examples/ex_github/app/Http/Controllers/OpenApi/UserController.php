<?php

// Code generated by lcodegen, DO NOT EDIT.

namespace App\Http\Controllers\OpenApi;

use App\Http\Controllers\Controller;
use App\Http\Requests\ActivityListEventsForAuthenticatedUserRequest;
use App\Http\Requests\ActivityListOrgEventsForAuthenticatedUserRequest;
use App\Http\Requests\ActivityListPublicEventsForUserRequest;
use App\Http\Requests\ActivityListReceivedEventsForUserRequest;
use App\Http\Requests\ActivityListReceivedPublicEventsForUserRequest;
use App\Http\Requests\ActivityListReposStarredByAuthenticatedUserRequest;
use App\Http\Requests\ActivityListReposWatchedByUserRequest;
use App\Http\Requests\ActivityListWatchedReposForAuthenticatedUserRequest;
use App\Http\Requests\AppsListInstallationReposForAuthenticatedUserRequest;
use App\Http\Requests\AppsListSubscriptionsForAuthenticatedUserRequest;
use App\Http\Requests\AppsListSubscriptionsForAuthenticatedUserStubbedRequest;
use App\Http\Requests\GistsListForUserRequest;
use App\Http\Requests\InteractionsSetRestrictionsForAuthenticatedUserRequest;
use App\Http\Requests\IssuesListForAuthenticatedUserRequest;
use App\Http\Requests\MigrationsGetStatusForAuthenticatedUserRequest;
use App\Http\Requests\MigrationsListForAuthenticatedUserRequest;
use App\Http\Requests\MigrationsListReposForUserRequest;
use App\Http\Requests\MigrationsStartForAuthenticatedUserRequest;
use App\Http\Requests\OrgsListForAuthenticatedUserRequest;
use App\Http\Requests\OrgsListForUserRequest;
use App\Http\Requests\OrgsListMembershipsForAuthenticatedUserRequest;
use App\Http\Requests\OrgsUpdateMembershipForAuthenticatedUserRequest;
use App\Http\Requests\PackagesGetAllPackageVersionsForPackageOwnedByAuthenticatedUserRequest;
use App\Http\Requests\PackagesListPackagesForAuthenticatedUserRequest;
use App\Http\Requests\PackagesListPackagesForUserRequest;
use App\Http\Requests\PackagesRestorePackageForAuthenticatedUserRequest;
use App\Http\Requests\PackagesRestorePackageForUserRequest;
use App\Http\Requests\ProjectsCreateForAuthenticatedUserRequest;
use App\Http\Requests\ProjectsListForUserRequest;
use App\Http\Requests\ReposCreateForAuthenticatedUserRequest;
use App\Http\Requests\ReposListForAuthenticatedUserRequest;
use App\Http\Requests\ReposListForUserRequest;
use App\Http\Requests\ReposListInvitationsForAuthenticatedUserRequest;
use App\Http\Requests\TeamsListForAuthenticatedUserRequest;
use App\Http\Requests\UsersAddEmailForAuthenticatedRequest;
use App\Http\Requests\UsersCreateGpgKeyForAuthenticatedRequest;
use App\Http\Requests\UsersCreatePublicSshKeyForAuthenticatedRequest;
use App\Http\Requests\UsersDeleteEmailForAuthenticatedRequest;
use App\Http\Requests\UsersGetContextForUserRequest;
use App\Http\Requests\UsersListRequest;
use App\Http\Requests\UsersListEmailsForAuthenticatedRequest;
use App\Http\Requests\UsersListFollowedByAuthenticatedRequest;
use App\Http\Requests\UsersListFollowersForAuthenticatedUserRequest;
use App\Http\Requests\UsersListFollowersForUserRequest;
use App\Http\Requests\UsersListFollowingForUserRequest;
use App\Http\Requests\UsersListGpgKeysForAuthenticatedRequest;
use App\Http\Requests\UsersListGpgKeysForUserRequest;
use App\Http\Requests\UsersListPublicEmailsForAuthenticatedRequest;
use App\Http\Requests\UsersListPublicKeysForUserRequest;
use App\Http\Requests\UsersListPublicSshKeysForAuthenticatedRequest;
use App\Http\Requests\UsersSetPrimaryEmailVisibilityForAuthenticatedRequest;
use App\Http\Requests\UsersUpdateAuthenticatedRequest;
use App\Http\Responses\OpenApi\ActivityCheckRepoIsStarredByAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\ActivityListEventsForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\ActivityListOrgEventsForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\ActivityListPublicEventsForUserResponse;
use App\Http\Responses\OpenApi\ActivityListReceivedEventsForUserResponse;
use App\Http\Responses\OpenApi\ActivityListReceivedPublicEventsForUserResponse;
use App\Http\Responses\OpenApi\ActivityListReposStarredByAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\ActivityListReposWatchedByUserResponse;
use App\Http\Responses\OpenApi\ActivityListWatchedReposForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\ActivityStarRepoForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\ActivityUnstarRepoForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\AppsAddRepoToInstallationResponse;
use App\Http\Responses\OpenApi\AppsListInstallationReposForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\AppsListSubscriptionsForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\AppsListSubscriptionsForAuthenticatedUserStubbedResponse;
use App\Http\Responses\OpenApi\AppsRemoveRepoFromInstallationResponse;
use App\Http\Responses\OpenApi\BillingGetGithubActionsBillingUserResponse;
use App\Http\Responses\OpenApi\BillingGetGithubPackagesBillingUserResponse;
use App\Http\Responses\OpenApi\BillingGetSharedStorageBillingUserResponse;
use App\Http\Responses\OpenApi\GistsListForUserResponse;
use App\Http\Responses\OpenApi\InteractionsRemoveRestrictionsForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\InteractionsSetRestrictionsForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\IssuesListForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\MigrationsDeleteArchiveForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\MigrationsGetStatusForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\MigrationsListForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\MigrationsListReposForUserResponse;
use App\Http\Responses\OpenApi\MigrationsStartForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\MigrationsUnlockRepoForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\OrgsGetMembershipForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\OrgsListForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\OrgsListForUserResponse;
use App\Http\Responses\OpenApi\OrgsListMembershipsForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\OrgsUpdateMembershipForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\PackagesDeletePackageForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\PackagesDeletePackageForUserResponse;
use App\Http\Responses\OpenApi\PackagesDeletePackageVersionForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\PackagesDeletePackageVersionForUserResponse;
use App\Http\Responses\OpenApi\PackagesGetAllPackageVersionsForPackageOwnedByAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\PackagesGetAllPackageVersionsForPackageOwnedByUserResponse;
use App\Http\Responses\OpenApi\PackagesGetPackageForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\PackagesGetPackageForUserResponse;
use App\Http\Responses\OpenApi\PackagesGetPackageVersionForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\PackagesGetPackageVersionForUserResponse;
use App\Http\Responses\OpenApi\PackagesListPackagesForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\PackagesListPackagesForUserResponse;
use App\Http\Responses\OpenApi\PackagesRestorePackageForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\PackagesRestorePackageForUserResponse;
use App\Http\Responses\OpenApi\PackagesRestorePackageVersionForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\PackagesRestorePackageVersionForUserResponse;
use App\Http\Responses\OpenApi\ProjectsCreateForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\ProjectsListForUserResponse;
use App\Http\Responses\OpenApi\ReposAcceptInvitationResponse;
use App\Http\Responses\OpenApi\ReposCreateForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\ReposDeclineInvitationResponse;
use App\Http\Responses\OpenApi\ReposListForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\ReposListForUserResponse;
use App\Http\Responses\OpenApi\ReposListInvitationsForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\TeamsListForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\UsersAddEmailForAuthenticatedResponse;
use App\Http\Responses\OpenApi\UsersBlockResponse;
use App\Http\Responses\OpenApi\UsersCheckBlockedResponse;
use App\Http\Responses\OpenApi\UsersCheckFollowingForUserResponse;
use App\Http\Responses\OpenApi\UsersCheckPersonIsFollowedByAuthenticatedResponse;
use App\Http\Responses\OpenApi\UsersCreateGpgKeyForAuthenticatedResponse;
use App\Http\Responses\OpenApi\UsersCreatePublicSshKeyForAuthenticatedResponse;
use App\Http\Responses\OpenApi\UsersDeleteEmailForAuthenticatedResponse;
use App\Http\Responses\OpenApi\UsersDeleteGpgKeyForAuthenticatedResponse;
use App\Http\Responses\OpenApi\UsersDeletePublicSshKeyForAuthenticatedResponse;
use App\Http\Responses\OpenApi\UsersFollowResponse;
use App\Http\Responses\OpenApi\UsersGetAuthenticatedResponse;
use App\Http\Responses\OpenApi\UsersGetByUsernameResponse;
use App\Http\Responses\OpenApi\UsersGetContextForUserResponse;
use App\Http\Responses\OpenApi\UsersGetGpgKeyForAuthenticatedResponse;
use App\Http\Responses\OpenApi\UsersGetPublicSshKeyForAuthenticatedResponse;
use App\Http\Responses\OpenApi\UsersListBlockedByAuthenticatedResponse;
use App\Http\Responses\OpenApi\UsersListEmailsForAuthenticatedResponse;
use App\Http\Responses\OpenApi\UsersListFollowedByAuthenticatedResponse;
use App\Http\Responses\OpenApi\UsersListFollowersForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\UsersListFollowersForUserResponse;
use App\Http\Responses\OpenApi\UsersListFollowingForUserResponse;
use App\Http\Responses\OpenApi\UsersListGpgKeysForAuthenticatedResponse;
use App\Http\Responses\OpenApi\UsersListGpgKeysForUserResponse;
use App\Http\Responses\OpenApi\UsersListPublicEmailsForAuthenticatedResponse;
use App\Http\Responses\OpenApi\UsersListPublicKeysForUserResponse;
use App\Http\Responses\OpenApi\UsersListPublicSshKeysForAuthenticatedResponse;
use App\Http\Responses\OpenApi\UsersListResponse;
use App\Http\Responses\OpenApi\UsersSetPrimaryEmailVisibilityForAuthenticatedResponse;
use App\Http\Responses\OpenApi\UsersUnblockResponse;
use App\Http\Responses\OpenApi\UsersUnfollowResponse;
use App\Http\Responses\OpenApi\UsersUpdateAuthenticatedResponse;

/**
 * Auto-generated abstract controller from OpenAPI specification.
 * Extend this class and implement the abstract methods.
 */
abstract class UserController extends Controller
{
    /**
     * GET /user/starred/{owner}/{repo}
     *
     * Check if a repository is starred by the authenticated user
     */
    abstract public function activityCheckRepoIsStarredByAuthenticatedUser(
        string $owner,
        string $repo,
    ): ActivityCheckRepoIsStarredByAuthenticatedUserResponse;

    /**
     * GET /users/{username}/events
     *
     * If you are authenticated as the given user, you will see your private events. Otherwise, you'll
     * only see public events.
     */
    abstract public function activityListEventsForAuthenticatedUser(
        ActivityListEventsForAuthenticatedUserRequest $request,
        string $username,
    ): ActivityListEventsForAuthenticatedUserResponse;

    /**
     * GET /users/{username}/events/orgs/{org}
     *
     * This is the user's organization dashboard. You must be authenticated as the user to view this.
     */
    abstract public function activityListOrgEventsForAuthenticatedUser(
        ActivityListOrgEventsForAuthenticatedUserRequest $request,
        string $username,
        string $org,
    ): ActivityListOrgEventsForAuthenticatedUserResponse;

    /**
     * GET /users/{username}/events/public
     *
     * List public events for a user
     */
    abstract public function activityListPublicEventsForUser(
        ActivityListPublicEventsForUserRequest $request,
        string $username,
    ): ActivityListPublicEventsForUserResponse;

    /**
     * GET /users/{username}/received_events
     *
     * These are events that you've received by watching repos and following users. If you are
     * authenticated as the given user, you will see private events. Otherwise, you'll only see public
     * events.
     */
    abstract public function activityListReceivedEventsForUser(
        ActivityListReceivedEventsForUserRequest $request,
        string $username,
    ): ActivityListReceivedEventsForUserResponse;

    /**
     * GET /users/{username}/received_events/public
     *
     * List public events received by a user
     */
    abstract public function activityListReceivedPublicEventsForUser(
        ActivityListReceivedPublicEventsForUserRequest $request,
        string $username,
    ): ActivityListReceivedPublicEventsForUserResponse;

    /**
     * GET /user/starred
     *
     * Lists repositories the authenticated user has starred.
     * You can also find out _when_ stars were created by passing the following custom [media
     * type](https://docs.github.com/rest/overview/media-types/) via the `Accept` header:.
     */
    abstract public function activityListReposStarredByAuthenticatedUser(
        ActivityListReposStarredByAuthenticatedUserRequest $request,
    ): ActivityListReposStarredByAuthenticatedUserResponse;

    /**
     * GET /users/{username}/subscriptions
     *
     * Lists repositories a user is watching.
     */
    abstract public function activityListReposWatchedByUser(
        ActivityListReposWatchedByUserRequest $request,
        string $username,
    ): ActivityListReposWatchedByUserResponse;

    /**
     * GET /user/subscriptions
     *
     * Lists repositories the authenticated user is watching.
     */
    abstract public function activityListWatchedReposForAuthenticatedUser(
        ActivityListWatchedReposForAuthenticatedUserRequest $request,
    ): ActivityListWatchedReposForAuthenticatedUserResponse;

    /**
     * PUT /user/starred/{owner}/{repo}
     *
     * Note that you'll need to set `Content-Length` to zero when calling out to this endpoint. For more
     * information, see "[HTTP verbs](https://docs.github.
     * com/rest/overview/resources-in-the-rest-api#http-verbs).".
     */
    abstract public function activityStarRepoForAuthenticatedUser(
        string $owner,
        string $repo,
    ): ActivityStarRepoForAuthenticatedUserResponse;

    /**
     * DELETE /user/starred/{owner}/{repo}
     *
     * Unstar a repository for the authenticated user
     */
    abstract public function activityUnstarRepoForAuthenticatedUser(
        string $owner,
        string $repo,
    ): ActivityUnstarRepoForAuthenticatedUserResponse;

    /**
     * PUT /user/installations/{installation_id}/repositories/{repository_id}
     *
     * Add a single repository to an installation. The authenticated user must have admin access to the
     * repository.
     * You must use a personal access token (which you can create via the [command line](https://docs.
     * github.com/github/authenticating-to-github/creating-a-personal-access-token) or [Basic
     * Authentication](https://docs.github.
     * com/rest/overview/other-authentication-methods#basic-authentication)) to access this endpoint.
     */
    abstract public function appsAddRepoToInstallation(
        int $installation_id,
        int $repository_id,
    ): AppsAddRepoToInstallationResponse;

    /**
     * GET /user/installations/{installation_id}/repositories
     *
     * List repositories that the authenticated user has explicit permission (`:read`, `:write`, or
     * `:admin`) to access for an installation.
     * The authenticated user has explicit permission to access repositories they own, repositories where
     * they are a collaborator, and repositories that they can access through an organization membership.
     * You must use a [user-to-server OAuth access token](https://docs.github.
     * com/apps/building-github-apps/identifying-and-authorizing-users-for-github-apps/#identifying-users-on-your-site), created for a user who has authorized your GitHub App, to access this endpoint.
     * The access the user has to each repository is included in the hash under the `permissions` key.
     */
    abstract public function appsListInstallationReposForAuthenticatedUser(
        AppsListInstallationReposForAuthenticatedUserRequest $request,
        int $installation_id,
    ): AppsListInstallationReposForAuthenticatedUserResponse;

    /**
     * GET /user/marketplace_purchases
     *
     * Lists the active subscriptions for the authenticated user. You must use a [user-to-server OAuth
     * access token](https://docs.github.
     * com/apps/building-github-apps/identifying-and-authorizing-users-for-github-apps/#identifying-users-on-your-site), created for a user who has authorized your GitHub App, to access this endpoint. . OAuth Apps must authenticate using an [OAuth token](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/).
     */
    abstract public function appsListSubscriptionsForAuthenticatedUser(
        AppsListSubscriptionsForAuthenticatedUserRequest $request,
    ): AppsListSubscriptionsForAuthenticatedUserResponse;

    /**
     * GET /user/marketplace_purchases/stubbed
     *
     * Lists the active subscriptions for the authenticated user. You must use a [user-to-server OAuth
     * access token](https://docs.github.
     * com/apps/building-github-apps/identifying-and-authorizing-users-for-github-apps/#identifying-users-on-your-site), created for a user who has authorized your GitHub App, to access this endpoint. . OAuth Apps must authenticate using an [OAuth token](https://docs.github.com/apps/building-github-apps/authenticating-with-github-apps/).
     */
    abstract public function appsListSubscriptionsForAuthenticatedUserStubbed(
        AppsListSubscriptionsForAuthenticatedUserStubbedRequest $request,
    ): AppsListSubscriptionsForAuthenticatedUserStubbedResponse;

    /**
     * DELETE /user/installations/{installation_id}/repositories/{repository_id}
     *
     * Remove a single repository from an installation. The authenticated user must have admin access to
     * the repository.
     * You must use a personal access token (which you can create via the [command line](https://docs.
     * github.com/github/authenticating-to-github/creating-a-personal-access-token) or [Basic
     * Authentication](https://docs.github.
     * com/rest/overview/other-authentication-methods#basic-authentication)) to access this endpoint.
     */
    abstract public function appsRemoveRepoFromInstallation(
        int $installation_id,
        int $repository_id,
    ): AppsRemoveRepoFromInstallationResponse;

    /**
     * GET /users/{username}/settings/billing/actions
     *
     * Gets the summary of the free and paid GitHub Actions minutes used.
     * Paid minutes only apply to workflows in private repositories that use GitHub-hosted runners.
     * Minutes used is listed for each GitHub-hosted runner operating system. Any job re-runs are also
     * included in the usage. The usage returned includes any minute multipliers for macOS and Windows
     * runners, and is rounded up to the nearest whole minute. For more information, see "[Managing
     * billing for GitHub Actions](https://help.github.
     * com/github/setting-up-and-managing-billing-and-payments-on-github/managing-billing-for-github-actions)".
     * Access tokens must have the `user` scope.
     */
    abstract public function billingGetGithubActionsBillingUser(
        string $username,
    ): BillingGetGithubActionsBillingUserResponse;

    /**
     * GET /users/{username}/settings/billing/packages
     *
     * Gets the free and paid storage used for GitHub Packages in gigabytes.
     * Paid minutes only apply to packages stored for private repositories. For more information, see
     * "[Managing billing for GitHub Packages](https://help.github.
     * com/github/setting-up-and-managing-billing-and-payments-on-github/managing-billing-for-github-packages)."
     * Access tokens must have the `user` scope.
     */
    abstract public function billingGetGithubPackagesBillingUser(
        string $username,
    ): BillingGetGithubPackagesBillingUserResponse;

    /**
     * GET /users/{username}/settings/billing/shared-storage
     *
     * Gets the estimated paid and estimated total storage used for GitHub Actions and Github Packages.
     * Paid minutes only apply to packages stored for private repositories. For more information, see
     * "[Managing billing for GitHub Packages](https://help.github.
     * com/github/setting-up-and-managing-billing-and-payments-on-github/managing-billing-for-github-packages)."
     * Access tokens must have the `user` scope.
     */
    abstract public function billingGetSharedStorageBillingUser(
        string $username,
    ): BillingGetSharedStorageBillingUserResponse;

    /**
     * GET /users/{username}/gists
     *
     * Lists public gists for the specified user:.
     */
    abstract public function gistsListForUser(
        GistsListForUserRequest $request,
        string $username,
    ): GistsListForUserResponse;

    /**
     * DELETE /user/interaction-limits
     *
     * Removes any interaction restrictions from your public repositories.
     */
    abstract public function interactionsRemoveRestrictionsForAuthenticatedUser(
    ): InteractionsRemoveRestrictionsForAuthenticatedUserResponse;

    /**
     * PUT /user/interaction-limits
     *
     * Temporarily restricts which type of GitHub user can interact with your public repositories.
     * Setting the interaction limit at the user level will overwrite any interaction limits that are set
     * for individual repositories owned by the user.
     */
    abstract public function interactionsSetRestrictionsForAuthenticatedUser(
        InteractionsSetRestrictionsForAuthenticatedUserRequest $request,
    ): InteractionsSetRestrictionsForAuthenticatedUserResponse;

    /**
     * GET /user/issues
     *
     * List issues across owned and member repositories assigned to the authenticated user.
     * **Note**: GitHub's REST API v3 considers every pull request an issue, but not every issue is a
     * pull request. For this
     * reason, "Issues" endpoints may return both issues and pull requests in the response. You can
     * identify pull requests by
     * the `pull_request` key. Be aware that the `id` of a pull request returned from "Issues" endpoints
     * will be an _issue id_. To find out the pull
     * request id, use the "[List pull requests](https://docs.github.
     * com/rest/reference/pulls#list-pull-requests)" endpoint.
     */
    abstract public function issuesListForAuthenticatedUser(
        IssuesListForAuthenticatedUserRequest $request,
    ): IssuesListForAuthenticatedUserResponse;

    /**
     * DELETE /user/migrations/{migration_id}/archive
     *
     * Deletes a previous migration archive. Downloadable migration archives are automatically deleted
     * after seven days. Migration metadata, which is returned in the [List user migrations](https://docs.
     * github.com/rest/reference/migrations#list-user-migrations) and [Get a user migration
     * status](https://docs.github.com/rest/reference/migrations#get-a-user-migration-status) endpoints,
     * will continue to be available even after an archive is deleted.
     */
    abstract public function migrationsDeleteArchiveForAuthenticatedUser(
        int $migration_id,
    ): MigrationsDeleteArchiveForAuthenticatedUserResponse;

    /**
     * GET /user/migrations/{migration_id}/archive
     *
     * Fetches the URL to download the migration archive as a `tar.gz` file. Depending on the resources
     * your repository uses, the migration archive can contain JSON files with data for these objects:
     * *   attachments
     * *   bases
     * *   commit\_comments
     * *   issue\_comments
     * *   issue\_events
     * *   issues
     * *   milestones
     * *   organizations
     * *   projects
     * *   protected\_branches
     * *   pull\_request\_reviews
     * *   pull\_requests
     * *   releases
     * *   repositories
     * *   review\_comments
     * *   schema
     * *   users
     * The archive will also contain an `attachments` directory that includes all attachment files
     * uploaded to GitHub.com and a `repositories` directory that contains the repository's Git data.
     */
    abstract public function migrationsGetArchiveForAuthenticatedUser(
        int $migration_id,
    ): void;

    /**
     * GET /user/migrations/{migration_id}
     *
     * Fetches a single user migration. The response includes the `state` of the migration, which can be
     * one of the following values:
     * *   `pending` - the migration hasn't started yet.
     * *   `exporting` - the migration is in progress.
     * *   `exported` - the migration finished successfully.
     * *   `failed` - the migration failed.
     * Once the migration has been `exported` you can [download the migration archive](https://docs.
     * github.com/rest/reference/migrations#download-a-user-migration-archive).
     */
    abstract public function migrationsGetStatusForAuthenticatedUser(
        MigrationsGetStatusForAuthenticatedUserRequest $request,
        int $migration_id,
    ): MigrationsGetStatusForAuthenticatedUserResponse;

    /**
     * GET /user/migrations
     *
     * Lists all migrations a user has started.
     */
    abstract public function migrationsListForAuthenticatedUser(
        MigrationsListForAuthenticatedUserRequest $request,
    ): MigrationsListForAuthenticatedUserResponse;

    /**
     * GET /user/migrations/{migration_id}/repositories
     *
     * Lists all the repositories for this user migration.
     */
    abstract public function migrationsListReposForUser(
        MigrationsListReposForUserRequest $request,
        int $migration_id,
    ): MigrationsListReposForUserResponse;

    /**
     * POST /user/migrations
     *
     * Initiates the generation of a user migration archive.
     */
    abstract public function migrationsStartForAuthenticatedUser(
        MigrationsStartForAuthenticatedUserRequest $request,
    ): MigrationsStartForAuthenticatedUserResponse;

    /**
     * DELETE /user/migrations/{migration_id}/repos/{repo_name}/lock
     *
     * Unlocks a repository. You can lock repositories when you [start a user migration](https://docs.
     * github.com/rest/reference/migrations#start-a-user-migration). Once the migration is complete you
     * can unlock each repository to begin using it again or [delete the repository](https://docs.github.
     * com/rest/reference/repos#delete-a-repository) if you no longer need the source data. Returns a
     * status of `404 Not Found` if the repository is not locked.
     */
    abstract public function migrationsUnlockRepoForAuthenticatedUser(
        int $migration_id,
        string $repo_name,
    ): MigrationsUnlockRepoForAuthenticatedUserResponse;

    /**
     * GET /user/memberships/orgs/{org}
     *
     * Get an organization membership for the authenticated user
     */
    abstract public function orgsGetMembershipForAuthenticatedUser(
        string $org,
    ): OrgsGetMembershipForAuthenticatedUserResponse;

    /**
     * GET /user/orgs
     *
     * List organizations for the authenticated user.
     * **OAuth scope requirements**
     * This only lists organizations that your authorization allows you to operate on in some way (e.g.,
     * you can list teams with `read:org` scope, you can publicize your organization membership with
     * `user` scope, etc.). Therefore, this API requires at least `user` or `read:org` scope. OAuth
     * requests with insufficient scope receive a `403 Forbidden` response.
     */
    abstract public function orgsListForAuthenticatedUser(
        OrgsListForAuthenticatedUserRequest $request,
    ): OrgsListForAuthenticatedUserResponse;

    /**
     * GET /users/{username}/orgs
     *
     * List [public organization memberships](https://help.github.
     * com/articles/publicizing-or-concealing-organization-membership) for the specified user.
     * This method only lists _public_ memberships, regardless of authentication. If you need to fetch
     * all of the organization memberships (public and private) for the authenticated user, use the [List
     * organizations for the authenticated user](https://docs.github.
     * com/rest/reference/orgs#list-organizations-for-the-authenticated-user) API instead.
     */
    abstract public function orgsListForUser(
        OrgsListForUserRequest $request,
        string $username,
    ): OrgsListForUserResponse;

    /**
     * GET /user/memberships/orgs
     *
     * List organization memberships for the authenticated user
     */
    abstract public function orgsListMembershipsForAuthenticatedUser(
        OrgsListMembershipsForAuthenticatedUserRequest $request,
    ): OrgsListMembershipsForAuthenticatedUserResponse;

    /**
     * PATCH /user/memberships/orgs/{org}
     *
     * Update an organization membership for the authenticated user
     */
    abstract public function orgsUpdateMembershipForAuthenticatedUser(
        OrgsUpdateMembershipForAuthenticatedUserRequest $request,
        string $org,
    ): OrgsUpdateMembershipForAuthenticatedUserResponse;

    /**
     * DELETE /user/packages/{package_type}/{package_name}
     *
     * Deletes a package owned by the authenticated user. You cannot delete a public package if any
     * version of the package has more than 5,000 downloads. In this scenario, contact GitHub support for
     * further assistance.
     * To use this endpoint, you must authenticate using an access token with the `packages:read` and
     * `packages:delete` scopes.
     * If `package_type` is not `container`, your token must also include the `repo` scope.
     */
    abstract public function packagesDeletePackageForAuthenticatedUser(
        PackageType $package_type,
        string $package_name,
    ): PackagesDeletePackageForAuthenticatedUserResponse;

    /**
     * DELETE /users/{username}/packages/{package_type}/{package_name}
     *
     * Deletes an entire package for a user. You cannot delete a public package if any version of the
     * package has more than 5,000 downloads. In this scenario, contact GitHub support for further
     * assistance.
     * To use this endpoint, you must authenticate using an access token with the `packages:read` and
     * `packages:delete` scopes. In addition:
     * - If `package_type` is not `container`, your token must also include the `repo` scope.
     * - If `package_type` is `container`, you must also have admin permissions to the container you want
     * to delete.
     */
    abstract public function packagesDeletePackageForUser(
        PackageType $package_type,
        string $package_name,
        string $username,
    ): PackagesDeletePackageForUserResponse;

    /**
     * DELETE /user/packages/{package_type}/{package_name}/versions/{package_version_id}
     *
     * Deletes a specific package version for a package owned by the authenticated user.  If the package
     * is public and the package version has more than 5,000 downloads, you cannot delete the package
     * version. In this scenario, contact GitHub support for further assistance.
     * To use this endpoint, you must have admin permissions in the organization and authenticate using
     * an access token with the `packages:read` and `packages:delete` scopes.
     * If `package_type` is not `container`, your token must also include the `repo` scope.
     */
    abstract public function packagesDeletePackageVersionForAuthenticatedUser(
        PackageType $package_type,
        string $package_name,
        int $package_version_id,
    ): PackagesDeletePackageVersionForAuthenticatedUserResponse;

    /**
     * DELETE /users/{username}/packages/{package_type}/{package_name}/versions/{package_version_id}
     *
     * Deletes a specific package version for a user. If the package is public and the package version
     * has more than 5,000 downloads, you cannot delete the package version. In this scenario, contact
     * GitHub support for further assistance.
     * To use this endpoint, you must authenticate using an access token with the `packages:read` and
     * `packages:delete` scopes. In addition:
     * - If `package_type` is not `container`, your token must also include the `repo` scope.
     * - If `package_type` is `container`, you must also have admin permissions to the container you want
     * to delete.
     */
    abstract public function packagesDeletePackageVersionForUser(
        PackageType $package_type,
        string $package_name,
        string $username,
        int $package_version_id,
    ): PackagesDeletePackageVersionForUserResponse;

    /**
     * GET /user/packages/{package_type}/{package_name}/versions
     *
     * Returns all package versions for a package owned by the authenticated user.
     * To use this endpoint, you must authenticate using an access token with the `packages:read` scope.
     * If `package_type` is not `container`, your token must also include the `repo` scope.
     */
    abstract public function packagesGetAllPackageVersionsForPackageOwnedByAuthenticatedUser(
        PackagesGetAllPackageVersionsForPackageOwnedByAuthenticatedUserRequest $request,
        PackageType $package_type,
        string $package_name,
    ): PackagesGetAllPackageVersionsForPackageOwnedByAuthenticatedUserResponse;

    /**
     * GET /users/{username}/packages/{package_type}/{package_name}/versions
     *
     * Returns all package versions for a public package owned by a specified user.
     * To use this endpoint, you must authenticate using an access token with the `packages:read` scope.
     * If `package_type` is not `container`, your token must also include the `repo` scope.
     */
    abstract public function packagesGetAllPackageVersionsForPackageOwnedByUser(
        PackageType $package_type,
        string $package_name,
        string $username,
    ): PackagesGetAllPackageVersionsForPackageOwnedByUserResponse;

    /**
     * GET /user/packages/{package_type}/{package_name}
     *
     * Gets a specific package for a package owned by the authenticated user.
     * To use this endpoint, you must authenticate using an access token with the `packages:read` scope.
     * If `package_type` is not `container`, your token must also include the `repo` scope.
     */
    abstract public function packagesGetPackageForAuthenticatedUser(
        PackageType $package_type,
        string $package_name,
    ): PackagesGetPackageForAuthenticatedUserResponse;

    /**
     * GET /users/{username}/packages/{package_type}/{package_name}
     *
     * Gets a specific package metadata for a public package owned by a user.
     * To use this endpoint, you must authenticate using an access token with the `packages:read` scope.
     * If `package_type` is not `container`, your token must also include the `repo` scope.
     */
    abstract public function packagesGetPackageForUser(
        PackageType $package_type,
        string $package_name,
        string $username,
    ): PackagesGetPackageForUserResponse;

    /**
     * GET /user/packages/{package_type}/{package_name}/versions/{package_version_id}
     *
     * Gets a specific package version for a package owned by the authenticated user.
     * To use this endpoint, you must authenticate using an access token with the `packages:read` scope.
     * If `package_type` is not `container`, your token must also include the `repo` scope.
     */
    abstract public function packagesGetPackageVersionForAuthenticatedUser(
        PackageType $package_type,
        string $package_name,
        int $package_version_id,
    ): PackagesGetPackageVersionForAuthenticatedUserResponse;

    /**
     * GET /users/{username}/packages/{package_type}/{package_name}/versions/{package_version_id}
     *
     * Gets a specific package version for a public package owned by a specified user.
     * At this time, to use this endpoint, you must authenticate using an access token with the
     * `packages:read` scope.
     * If `package_type` is not `container`, your token must also include the `repo` scope.
     */
    abstract public function packagesGetPackageVersionForUser(
        PackageType $package_type,
        string $package_name,
        int $package_version_id,
        string $username,
    ): PackagesGetPackageVersionForUserResponse;

    /**
     * GET /user/packages
     *
     * Lists packages owned by the authenticated user within the user's namespace.
     * To use this endpoint, you must authenticate using an access token with the `packages:read` scope.
     * If `package_type` is not `container`, your token must also include the `repo` scope.
     */
    abstract public function packagesListPackagesForAuthenticatedUser(
        PackagesListPackagesForAuthenticatedUserRequest $request,
    ): PackagesListPackagesForAuthenticatedUserResponse;

    /**
     * GET /users/{username}/packages
     *
     * Lists all packages in a user's namespace for which the requesting user has access.
     * To use this endpoint, you must authenticate using an access token with the `packages:read` scope.
     * If `package_type` is not `container`, your token must also include the `repo` scope.
     */
    abstract public function packagesListPackagesForUser(
        PackagesListPackagesForUserRequest $request,
        string $username,
    ): PackagesListPackagesForUserResponse;

    /**
     * POST /user/packages/{package_type}/{package_name}/restore
     *
     * Restores a package owned by the authenticated user.
     * You can restore a deleted package under the following conditions:
     * - The package was deleted within the last 30 days.
     * - The same package namespace and version is still available and not reused for a new package. If
     * the same package namespace is not available, you will not be able to restore your package. In this
     * scenario, to restore the deleted package, you must delete the new package that uses the deleted
     * package's namespace first.
     * To use this endpoint, you must authenticate using an access token with the `packages:read` and
     * `packages:write` scopes. If `package_type` is not `container`, your token must also include the
     * `repo` scope.
     */
    abstract public function packagesRestorePackageForAuthenticatedUser(
        PackagesRestorePackageForAuthenticatedUserRequest $request,
        PackageType $package_type,
        string $package_name,
    ): PackagesRestorePackageForAuthenticatedUserResponse;

    /**
     * POST /users/{username}/packages/{package_type}/{package_name}/restore
     *
     * Restores an entire package for a user.
     * You can restore a deleted package under the following conditions:
     * - The package was deleted within the last 30 days.
     * - The same package namespace and version is still available and not reused for a new package. If
     * the same package namespace is not available, you will not be able to restore your package. In this
     * scenario, to restore the deleted package, you must delete the new package that uses the deleted
     * package's namespace first.
     * To use this endpoint, you must authenticate using an access token with the `packages:read` and
     * `packages:write` scopes. In addition:
     * - If `package_type` is not `container`, your token must also include the `repo` scope.
     * - If `package_type` is `container`, you must also have admin permissions to the container that you
     * want to restore.
     */
    abstract public function packagesRestorePackageForUser(
        PackagesRestorePackageForUserRequest $request,
        PackageType $package_type,
        string $package_name,
        string $username,
    ): PackagesRestorePackageForUserResponse;

    /**
     * POST /user/packages/{package_type}/{package_name}/versions/{package_version_id}/restore
     *
     * Restores a package version owned by the authenticated user.
     * You can restore a deleted package version under the following conditions:
     * - The package was deleted within the last 30 days.
     * - The same package namespace and version is still available and not reused for a new package. If
     * the same package namespace is not available, you will not be able to restore your package. In this
     * scenario, to restore the deleted package, you must delete the new package that uses the deleted
     * package's namespace first.
     * To use this endpoint, you must authenticate using an access token with the `packages:read` and
     * `packages:write` scopes. If `package_type` is not `container`, your token must also include the
     * `repo` scope.
     */
    abstract public function packagesRestorePackageVersionForAuthenticatedUser(
        PackageType $package_type,
        string $package_name,
        int $package_version_id,
    ): PackagesRestorePackageVersionForAuthenticatedUserResponse;

    /**
     * POST /users/{username}/packages/{package_type}/{package_name}/versions/{package_version_id}/restore
     *
     * Restores a specific package version for a user.
     * You can restore a deleted package under the following conditions:
     * - The package was deleted within the last 30 days.
     * - The same package namespace and version is still available and not reused for a new package. If
     * the same package namespace is not available, you will not be able to restore your package. In this
     * scenario, to restore the deleted package, you must delete the new package that uses the deleted
     * package's namespace first.
     * To use this endpoint, you must authenticate using an access token with the `packages:read` and
     * `packages:write` scopes. In addition:
     * - If `package_type` is not `container`, your token must also include the `repo` scope.
     * - If `package_type` is `container`, you must also have admin permissions to the container that you
     * want to restore.
     */
    abstract public function packagesRestorePackageVersionForUser(
        PackageType $package_type,
        string $package_name,
        string $username,
        int $package_version_id,
    ): PackagesRestorePackageVersionForUserResponse;

    /**
     * POST /user/projects
     *
     * Create a user project
     */
    abstract public function projectsCreateForAuthenticatedUser(
        ProjectsCreateForAuthenticatedUserRequest $request,
    ): ProjectsCreateForAuthenticatedUserResponse;

    /**
     * GET /users/{username}/projects
     *
     * List user projects
     */
    abstract public function projectsListForUser(
        ProjectsListForUserRequest $request,
        string $username,
    ): ProjectsListForUserResponse;

    /**
     * PATCH /user/repository_invitations/{invitation_id}
     *
     * Accept a repository invitation
     */
    abstract public function reposAcceptInvitation(
        int $invitation_id,
    ): ReposAcceptInvitationResponse;

    /**
     * POST /user/repos
     *
     * Creates a new repository for the authenticated user.
     * **OAuth scope requirements**
     * When using [OAuth](https://docs.github.
     * com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/), authorizations must include:
     * *   `public_repo` scope or `repo` scope to create a public repository. Note: For GitHub AE, use
     * `repo` scope to create an internal repository.
     * *   `repo` scope to create a private repository.
     */
    abstract public function reposCreateForAuthenticatedUser(
        ReposCreateForAuthenticatedUserRequest $request,
    ): ReposCreateForAuthenticatedUserResponse;

    /**
     * DELETE /user/repository_invitations/{invitation_id}
     *
     * Decline a repository invitation
     */
    abstract public function reposDeclineInvitation(
        int $invitation_id,
    ): ReposDeclineInvitationResponse;

    /**
     * GET /user/repos
     *
     * Lists repositories that the authenticated user has explicit permission (`:read`, `:write`, or
     * `:admin`) to access.
     * The authenticated user has explicit permission to access repositories they own, repositories where
     * they are a collaborator, and repositories that they can access through an organization membership.
     */
    abstract public function reposListForAuthenticatedUser(
        ReposListForAuthenticatedUserRequest $request,
    ): ReposListForAuthenticatedUserResponse;

    /**
     * GET /users/{username}/repos
     *
     * Lists public repositories for the specified user. Note: For GitHub AE, this endpoint will list
     * internal repositories for the specified user.
     */
    abstract public function reposListForUser(
        ReposListForUserRequest $request,
        string $username,
    ): ReposListForUserResponse;

    /**
     * GET /user/repository_invitations
     *
     * When authenticating as a user, this endpoint will list all currently open repository invitations
     * for that user.
     */
    abstract public function reposListInvitationsForAuthenticatedUser(
        ReposListInvitationsForAuthenticatedUserRequest $request,
    ): ReposListInvitationsForAuthenticatedUserResponse;

    /**
     * GET /user/teams
     *
     * List all of the teams across all of the organizations to which the authenticated user belongs.
     * This method requires `user`, `repo`, or `read:org` [scope](https://docs.github.
     * com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/) when authenticating via
     * [OAuth](https://docs.github.com/apps/building-oauth-apps/).
     */
    abstract public function teamsListForAuthenticatedUser(
        TeamsListForAuthenticatedUserRequest $request,
    ): TeamsListForAuthenticatedUserResponse;

    /**
     * POST /user/emails
     *
     * This endpoint is accessible with the `user` scope.
     */
    abstract public function usersAddEmailForAuthenticated(
        UsersAddEmailForAuthenticatedRequest $request,
    ): UsersAddEmailForAuthenticatedResponse;

    /**
     * PUT /user/blocks/{username}
     *
     * Block a user
     */
    abstract public function usersBlock(
        string $username,
    ): UsersBlockResponse;

    /**
     * GET /user/blocks/{username}
     *
     * Check if a user is blocked by the authenticated user
     */
    abstract public function usersCheckBlocked(
        string $username,
    ): UsersCheckBlockedResponse;

    /**
     * GET /users/{username}/following/{target_user}
     *
     * Check if a user follows another user
     */
    abstract public function usersCheckFollowingForUser(
        string $username,
        string $target_user,
    ): UsersCheckFollowingForUserResponse;

    /**
     * GET /user/following/{username}
     *
     * Check if a person is followed by the authenticated user
     */
    abstract public function usersCheckPersonIsFollowedByAuthenticated(
        string $username,
    ): UsersCheckPersonIsFollowedByAuthenticatedResponse;

    /**
     * POST /user/gpg_keys
     *
     * Adds a GPG key to the authenticated user's GitHub account. Requires that you are authenticated via
     * Basic Auth, or OAuth with at least `write:gpg_key` [scope](https://docs.github.
     * com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     */
    abstract public function usersCreateGpgKeyForAuthenticated(
        UsersCreateGpgKeyForAuthenticatedRequest $request,
    ): UsersCreateGpgKeyForAuthenticatedResponse;

    /**
     * POST /user/keys
     *
     * Adds a public SSH key to the authenticated user's GitHub account. Requires that you are
     * authenticated via Basic Auth, or OAuth with at least `write:public_key` [scope](https://docs.
     * github.com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     */
    abstract public function usersCreatePublicSshKeyForAuthenticated(
        UsersCreatePublicSshKeyForAuthenticatedRequest $request,
    ): UsersCreatePublicSshKeyForAuthenticatedResponse;

    /**
     * DELETE /user/emails
     *
     * This endpoint is accessible with the `user` scope.
     */
    abstract public function usersDeleteEmailForAuthenticated(
        UsersDeleteEmailForAuthenticatedRequest $request,
    ): UsersDeleteEmailForAuthenticatedResponse;

    /**
     * DELETE /user/gpg_keys/{gpg_key_id}
     *
     * Removes a GPG key from the authenticated user's GitHub account. Requires that you are
     * authenticated via Basic Auth or via OAuth with at least `admin:gpg_key` [scope](https://docs.
     * github.com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     */
    abstract public function usersDeleteGpgKeyForAuthenticated(
        int $gpg_key_id,
    ): UsersDeleteGpgKeyForAuthenticatedResponse;

    /**
     * DELETE /user/keys/{key_id}
     *
     * Removes a public SSH key from the authenticated user's GitHub account. Requires that you are
     * authenticated via Basic Auth or via OAuth with at least `admin:public_key` [scope](https://docs.
     * github.com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     */
    abstract public function usersDeletePublicSshKeyForAuthenticated(
        int $key_id,
    ): UsersDeletePublicSshKeyForAuthenticatedResponse;

    /**
     * PUT /user/following/{username}
     *
     * Note that you'll need to set `Content-Length` to zero when calling out to this endpoint. For more
     * information, see "[HTTP verbs](https://docs.github.
     * com/rest/overview/resources-in-the-rest-api#http-verbs)."
     * Following a user requires the user to be logged in and authenticated with basic auth or OAuth with
     * the `user:follow` scope.
     */
    abstract public function usersFollow(
        string $username,
    ): UsersFollowResponse;

    /**
     * GET /user
     *
     * If the authenticated user is authenticated through basic authentication or OAuth with the `user`
     * scope, then the response lists public and private profile information.
     * If the authenticated user is authenticated through OAuth without the `user` scope, then the
     * response lists only public profile information.
     */
    abstract public function usersGetAuthenticated(
    ): UsersGetAuthenticatedResponse;

    /**
     * GET /users/{username}
     *
     * Provides publicly available information about someone with a GitHub account.
     * GitHub Apps with the `Plan` user permission can use this endpoint to retrieve information about a
     * user's GitHub plan. The GitHub App must be authenticated as a user. See "[Identifying and
     * authorizing users for GitHub Apps](https://docs.github.
     * com/apps/building-github-apps/identifying-and-authorizing-users-for-github-apps/)" for details
     * about authentication. For an example response, see 'Response with GitHub plan information' below"
     * The `email` key in the following response is the publicly visible email address from your GitHub
     * [profile page](https://github.com/settings/profile). When setting up your profile, you can select
     * a primary email address to be “public” which provides an email entry for this endpoint. If you
     * do not set a public email address for `email`, then it will have a value of `null`. You only see
     * publicly visible email addresses when authenticated with GitHub. For more information, see
     * [Authentication](https://docs.github.com/rest/overview/resources-in-the-rest-api#authentication).
     * The Emails API enables you to list all of your email addresses, and toggle a primary email to be
     * visible publicly. For more information, see "[Emails API](https://docs.github.
     * com/rest/reference/users#emails)".
     */
    abstract public function usersGetByUsername(
        string $username,
    ): UsersGetByUsernameResponse;

    /**
     * GET /users/{username}/hovercard
     *
     * Provides hovercard information when authenticated through basic auth or OAuth with the `repo`
     * scope. You can find out more about someone in relation to their pull requests, issues,
     * repositories, and organizations.
     * The `subject_type` and `subject_id` parameters provide context for the person's hovercard, which
     * returns more information than without the parameters. For example, if you wanted to find out more
     * about `octocat` who owns the `Spoon-Knife` repository via cURL, it would look like this:
     * ```shell
     * curl -u username:token
     * https://api.github.com/users/octocat/hovercard?subject_type=repository&subject_id=1300192
     * ```.
     */
    abstract public function usersGetContextForUser(
        UsersGetContextForUserRequest $request,
        string $username,
    ): UsersGetContextForUserResponse;

    /**
     * GET /user/gpg_keys/{gpg_key_id}
     *
     * View extended details for a single GPG key. Requires that you are authenticated via Basic Auth or
     * via OAuth with at least `read:gpg_key` [scope](https://docs.github.
     * com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     */
    abstract public function usersGetGpgKeyForAuthenticated(
        int $gpg_key_id,
    ): UsersGetGpgKeyForAuthenticatedResponse;

    /**
     * GET /user/keys/{key_id}
     *
     * View extended details for a single public SSH key. Requires that you are authenticated via Basic
     * Auth or via OAuth with at least `read:public_key` [scope](https://docs.github.
     * com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     */
    abstract public function usersGetPublicSshKeyForAuthenticated(
        int $key_id,
    ): UsersGetPublicSshKeyForAuthenticatedResponse;

    /**
     * GET /users
     *
     * Lists all users, in the order that they signed up on GitHub. This list includes personal user
     * accounts and organization accounts.
     * Note: Pagination is powered exclusively by the `since` parameter. Use the [Link
     * header](https://docs.github.com/rest/overview/resources-in-the-rest-api#link-header) to get the
     * URL for the next page of users.
     */
    abstract public function usersList(
        UsersListRequest $request,
    ): UsersListResponse;

    /**
     * GET /user/blocks
     *
     * List the users you've blocked on your personal account.
     */
    abstract public function usersListBlockedByAuthenticated(
    ): UsersListBlockedByAuthenticatedResponse;

    /**
     * GET /user/emails
     *
     * Lists all of your email addresses, and specifies which one is visible to the public. This endpoint
     * is accessible with the `user:email` scope.
     */
    abstract public function usersListEmailsForAuthenticated(
        UsersListEmailsForAuthenticatedRequest $request,
    ): UsersListEmailsForAuthenticatedResponse;

    /**
     * GET /user/following
     *
     * Lists the people who the authenticated user follows.
     */
    abstract public function usersListFollowedByAuthenticated(
        UsersListFollowedByAuthenticatedRequest $request,
    ): UsersListFollowedByAuthenticatedResponse;

    /**
     * GET /user/followers
     *
     * Lists the people following the authenticated user.
     */
    abstract public function usersListFollowersForAuthenticatedUser(
        UsersListFollowersForAuthenticatedUserRequest $request,
    ): UsersListFollowersForAuthenticatedUserResponse;

    /**
     * GET /users/{username}/followers
     *
     * Lists the people following the specified user.
     */
    abstract public function usersListFollowersForUser(
        UsersListFollowersForUserRequest $request,
        string $username,
    ): UsersListFollowersForUserResponse;

    /**
     * GET /users/{username}/following
     *
     * Lists the people who the specified user follows.
     */
    abstract public function usersListFollowingForUser(
        UsersListFollowingForUserRequest $request,
        string $username,
    ): UsersListFollowingForUserResponse;

    /**
     * GET /user/gpg_keys
     *
     * Lists the current user's GPG keys. Requires that you are authenticated via Basic Auth or via OAuth
     * with at least `read:gpg_key` [scope](https://docs.github.
     * com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     */
    abstract public function usersListGpgKeysForAuthenticated(
        UsersListGpgKeysForAuthenticatedRequest $request,
    ): UsersListGpgKeysForAuthenticatedResponse;

    /**
     * GET /users/{username}/gpg_keys
     *
     * Lists the GPG keys for a user. This information is accessible by anyone.
     */
    abstract public function usersListGpgKeysForUser(
        UsersListGpgKeysForUserRequest $request,
        string $username,
    ): UsersListGpgKeysForUserResponse;

    /**
     * GET /user/public_emails
     *
     * Lists your publicly visible email address, which you can set with the [Set primary email
     * visibility for the authenticated user](https://docs.github.
     * com/rest/reference/users#set-primary-email-visibility-for-the-authenticated-user) endpoint. This
     * endpoint is accessible with the `user:email` scope.
     */
    abstract public function usersListPublicEmailsForAuthenticated(
        UsersListPublicEmailsForAuthenticatedRequest $request,
    ): UsersListPublicEmailsForAuthenticatedResponse;

    /**
     * GET /users/{username}/keys
     *
     * Lists the _verified_ public SSH keys for a user. This is accessible by anyone.
     */
    abstract public function usersListPublicKeysForUser(
        UsersListPublicKeysForUserRequest $request,
        string $username,
    ): UsersListPublicKeysForUserResponse;

    /**
     * GET /user/keys
     *
     * Lists the public SSH keys for the authenticated user's GitHub account. Requires that you are
     * authenticated via Basic Auth or via OAuth with at least `read:public_key` [scope](https://docs.
     * github.com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     */
    abstract public function usersListPublicSshKeysForAuthenticated(
        UsersListPublicSshKeysForAuthenticatedRequest $request,
    ): UsersListPublicSshKeysForAuthenticatedResponse;

    /**
     * PATCH /user/email/visibility
     *
     * Sets the visibility for your primary email addresses.
     */
    abstract public function usersSetPrimaryEmailVisibilityForAuthenticated(
        UsersSetPrimaryEmailVisibilityForAuthenticatedRequest $request,
    ): UsersSetPrimaryEmailVisibilityForAuthenticatedResponse;

    /**
     * DELETE /user/blocks/{username}
     *
     * Unblock a user
     */
    abstract public function usersUnblock(
        string $username,
    ): UsersUnblockResponse;

    /**
     * DELETE /user/following/{username}
     *
     * Unfollowing a user requires the user to be logged in and authenticated with basic auth or OAuth
     * with the `user:follow` scope.
     */
    abstract public function usersUnfollow(
        string $username,
    ): UsersUnfollowResponse;

    /**
     * PATCH /user
     *
     * **Note:** If your email is set to private and you send an `email` parameter as part of this
     * request to update your profile, your privacy settings are still enforced: the email address will
     * not be displayed on your public profile or via the API.
     */
    abstract public function usersUpdateAuthenticated(
        UsersUpdateAuthenticatedRequest $request,
    ): UsersUpdateAuthenticatedResponse;
}
