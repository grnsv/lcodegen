<?php

// Code generated by lcodegen, DO NOT EDIT.

namespace App\Http\Controllers\OpenApi;

use App\Http\Controllers\Controller;
use App\Http\Requests\EnterpriseAdminGetProvisioningInformationForEnterpriseGroupRequest;
use App\Http\Requests\EnterpriseAdminListProvisionedGroupsEnterpriseRequest;
use App\Http\Requests\EnterpriseAdminListProvisionedIdentitiesEnterpriseRequest;
use App\Http\Requests\EnterpriseAdminProvisionAndInviteEnterpriseGroupRequest;
use App\Http\Requests\EnterpriseAdminProvisionAndInviteEnterpriseUserRequest;
use App\Http\Requests\EnterpriseAdminSetInformationForProvisionedEnterpriseGroupRequest;
use App\Http\Requests\EnterpriseAdminSetInformationForProvisionedEnterpriseUserRequest;
use App\Http\Requests\EnterpriseAdminUpdateAttributeForEnterpriseGroupRequest;
use App\Http\Requests\EnterpriseAdminUpdateAttributeForEnterpriseUserRequest;
use App\Http\Requests\ScimListProvisionedIdentitiesRequest;
use App\Http\Requests\ScimProvisionAndInviteUserRequest;
use App\Http\Requests\ScimSetInformationForProvisionedUserRequest;
use App\Http\Requests\ScimUpdateAttributeForUserRequest;
use App\Http\Responses\OpenApi\EnterpriseAdminDeleteScimGroupFromEnterpriseResponse;
use App\Http\Responses\OpenApi\EnterpriseAdminDeleteUserFromEnterpriseResponse;
use App\Http\Responses\OpenApi\EnterpriseAdminGetProvisioningInformationForEnterpriseGroupResponse;
use App\Http\Responses\OpenApi\EnterpriseAdminGetProvisioningInformationForEnterpriseUserResponse;
use App\Http\Responses\OpenApi\EnterpriseAdminListProvisionedGroupsEnterpriseResponse;
use App\Http\Responses\OpenApi\EnterpriseAdminListProvisionedIdentitiesEnterpriseResponse;
use App\Http\Responses\OpenApi\EnterpriseAdminProvisionAndInviteEnterpriseGroupResponse;
use App\Http\Responses\OpenApi\EnterpriseAdminProvisionAndInviteEnterpriseUserResponse;
use App\Http\Responses\OpenApi\EnterpriseAdminSetInformationForProvisionedEnterpriseGroupResponse;
use App\Http\Responses\OpenApi\EnterpriseAdminSetInformationForProvisionedEnterpriseUserResponse;
use App\Http\Responses\OpenApi\EnterpriseAdminUpdateAttributeForEnterpriseGroupResponse;
use App\Http\Responses\OpenApi\EnterpriseAdminUpdateAttributeForEnterpriseUserResponse;
use App\Http\Responses\OpenApi\ScimDeleteUserFromOrgResponse;
use App\Http\Responses\OpenApi\ScimGetProvisioningInformationForUserResponse;
use App\Http\Responses\OpenApi\ScimListProvisionedIdentitiesResponse;
use App\Http\Responses\OpenApi\ScimProvisionAndInviteUserResponse;
use App\Http\Responses\OpenApi\ScimSetInformationForProvisionedUserResponse;
use App\Http\Responses\OpenApi\ScimUpdateAttributeForUserResponse;

/**
 * Auto-generated abstract controller from OpenAPI specification.
 * Extend this class and implement the abstract methods.
 */
abstract class ScimController extends Controller
{
    /**
     * DELETE /scim/v2/enterprises/{enterprise}/Groups/{scim_group_id}
     *
     * **Note:** The SCIM API endpoints for enterprise accounts are currently in beta and are subject to
     * change.
     */
    abstract public function enterpriseAdminDeleteScimGroupFromEnterprise(
        string $enterprise,
        string $scim_group_id,
    ): EnterpriseAdminDeleteScimGroupFromEnterpriseResponse;

    /**
     * DELETE /scim/v2/enterprises/{enterprise}/Users/{scim_user_id}
     *
     * **Note:** The SCIM API endpoints for enterprise accounts are currently in beta and are subject to
     * change.
     */
    abstract public function enterpriseAdminDeleteUserFromEnterprise(
        string $enterprise,
        string $scim_user_id,
    ): EnterpriseAdminDeleteUserFromEnterpriseResponse;

    /**
     * GET /scim/v2/enterprises/{enterprise}/Groups/{scim_group_id}
     *
     * **Note:** The SCIM API endpoints for enterprise accounts are currently in beta and are subject to
     * change.
     */
    abstract public function enterpriseAdminGetProvisioningInformationForEnterpriseGroup(
        EnterpriseAdminGetProvisioningInformationForEnterpriseGroupRequest $request,
        string $enterprise,
        string $scim_group_id,
    ): EnterpriseAdminGetProvisioningInformationForEnterpriseGroupResponse;

    /**
     * GET /scim/v2/enterprises/{enterprise}/Users/{scim_user_id}
     *
     * **Note:** The SCIM API endpoints for enterprise accounts are currently in beta and are subject to
     * change.
     */
    abstract public function enterpriseAdminGetProvisioningInformationForEnterpriseUser(
        string $enterprise,
        string $scim_user_id,
    ): EnterpriseAdminGetProvisioningInformationForEnterpriseUserResponse;

    /**
     * GET /scim/v2/enterprises/{enterprise}/Groups
     *
     * **Note:** The SCIM API endpoints for enterprise accounts are currently in beta and are subject to
     * change.
     */
    abstract public function enterpriseAdminListProvisionedGroupsEnterprise(
        EnterpriseAdminListProvisionedGroupsEnterpriseRequest $request,
        string $enterprise,
    ): EnterpriseAdminListProvisionedGroupsEnterpriseResponse;

    /**
     * GET /scim/v2/enterprises/{enterprise}/Users
     *
     * **Note:** The SCIM API endpoints for enterprise accounts are currently in beta and are subject to
     * change.
     * Retrieves a paginated list of all provisioned enterprise members, including pending invitations.
     * When a user with a SAML-provisioned external identity leaves (or is removed from) an enterprise,
     * the account's metadata is immediately removed. However, the returned list of user accounts might
     * not always match the organization or enterprise member list you see on GitHub. This can happen in
     * certain cases where an external identity associated with an organization will not match an
     * organization member:
     * - When a user with a SCIM-provisioned external identity is removed from an enterprise, the
     * account's metadata is preserved to allow the user to re-join the organization in the future.
     * - When inviting a user to join an organization, you can expect to see their external identity in
     * the results before they accept the invitation, or if the invitation is cancelled (or never
     * accepted).
     * - When a user is invited over SCIM, an external identity is created that matches with the
     * invitee's email address. However, this identity is only linked to a user account when the user
     * accepts the invitation by going through SAML SSO.
     * The returned list of external identities can include an entry for a `null` user. These are
     * unlinked SAML identities that are created when a user goes through the following Single Sign-On
     * (SSO) process but does not sign in to their GitHub account after completing SSO:
     * 1. The user is granted access by the IdP and is not a member of the GitHub enterprise.
     * 1. The user attempts to access the GitHub enterprise and initiates the SAML SSO process, and is
     * not currently signed in to their GitHub account.
     * 1. After successfully authenticating with the SAML SSO IdP, the `null` external identity entry is
     * created and the user is prompted to sign in to their GitHub account:
     * - If the user signs in, their GitHub account is linked to this entry.
     * - If the user does not sign in (or does not create a new account when prompted), they are not
     * added to the GitHub enterprise, and the external identity `null` entry remains in place.
     */
    abstract public function enterpriseAdminListProvisionedIdentitiesEnterprise(
        EnterpriseAdminListProvisionedIdentitiesEnterpriseRequest $request,
        string $enterprise,
    ): EnterpriseAdminListProvisionedIdentitiesEnterpriseResponse;

    /**
     * POST /scim/v2/enterprises/{enterprise}/Groups
     *
     * **Note:** The SCIM API endpoints for enterprise accounts are currently in beta and are subject to
     * change.
     * Provision an enterprise group, and invite users to the group. This sends invitation emails to the
     * email address of the invited users to join the GitHub organization that the SCIM group corresponds
     * to.
     */
    abstract public function enterpriseAdminProvisionAndInviteEnterpriseGroup(
        EnterpriseAdminProvisionAndInviteEnterpriseGroupRequest $request,
        string $enterprise,
    ): EnterpriseAdminProvisionAndInviteEnterpriseGroupResponse;

    /**
     * POST /scim/v2/enterprises/{enterprise}/Users
     *
     * **Note:** The SCIM API endpoints for enterprise accounts are currently in beta and are subject to
     * change.
     * Provision enterprise membership for a user, and send organization invitation emails to the email
     * address.
     * You can optionally include the groups a user will be invited to join. If you do not provide a list
     * of `groups`, the user is provisioned for the enterprise, but no organization invitation emails
     * will be sent.
     */
    abstract public function enterpriseAdminProvisionAndInviteEnterpriseUser(
        EnterpriseAdminProvisionAndInviteEnterpriseUserRequest $request,
        string $enterprise,
    ): EnterpriseAdminProvisionAndInviteEnterpriseUserResponse;

    /**
     * PUT /scim/v2/enterprises/{enterprise}/Groups/{scim_group_id}
     *
     * **Note:** The SCIM API endpoints for enterprise accounts are currently in beta and are subject to
     * change.
     * Replaces an existing provisioned group’s information. You must provide all the information
     * required for the group as if you were provisioning it for the first time. Any existing group
     * information that you don't provide will be removed, including group membership. If you want to
     * only update a specific attribute, use the [Update an attribute for a SCIM enterprise
     * group](#update-an-attribute-for-a-scim-enterprise-group) endpoint instead.
     */
    abstract public function enterpriseAdminSetInformationForProvisionedEnterpriseGroup(
        EnterpriseAdminSetInformationForProvisionedEnterpriseGroupRequest $request,
        string $enterprise,
        string $scim_group_id,
    ): EnterpriseAdminSetInformationForProvisionedEnterpriseGroupResponse;

    /**
     * PUT /scim/v2/enterprises/{enterprise}/Users/{scim_user_id}
     *
     * **Note:** The SCIM API endpoints for enterprise accounts are currently in beta and are subject to
     * change.
     * Replaces an existing provisioned user's information. You must provide all the information required
     * for the user as if you were provisioning them for the first time. Any existing user information
     * that you don't provide will be removed. If you want to only update a specific attribute, use the
     * [Update an attribute for a SCIM user](#update-an-attribute-for-an-enterprise-scim-user) endpoint
     * instead.
     * You must at least provide the required values for the user: `userName`, `name`, and `emails`.
     * **Warning:** Setting `active: false` removes the user from the enterprise, deletes the external
     * identity, and deletes the associated `{scim_user_id}`.
     */
    abstract public function enterpriseAdminSetInformationForProvisionedEnterpriseUser(
        EnterpriseAdminSetInformationForProvisionedEnterpriseUserRequest $request,
        string $enterprise,
        string $scim_user_id,
    ): EnterpriseAdminSetInformationForProvisionedEnterpriseUserResponse;

    /**
     * PATCH /scim/v2/enterprises/{enterprise}/Groups/{scim_group_id}
     *
     * **Note:** The SCIM API endpoints for enterprise accounts are currently in beta and are subject to
     * change.
     * Allows you to change a provisioned group’s individual attributes. To change a group’s values,
     * you must provide a specific Operations JSON format that contains at least one of the add, remove,
     * or replace operations. For examples and more information on the SCIM operations format, see the
     * [SCIM specification](https://tools.ietf.org/html/rfc7644#section-3.5.2).
     */
    abstract public function enterpriseAdminUpdateAttributeForEnterpriseGroup(
        EnterpriseAdminUpdateAttributeForEnterpriseGroupRequest $request,
        string $enterprise,
        string $scim_group_id,
    ): EnterpriseAdminUpdateAttributeForEnterpriseGroupResponse;

    /**
     * PATCH /scim/v2/enterprises/{enterprise}/Users/{scim_user_id}
     *
     * **Note:** The SCIM API endpoints for enterprise accounts are currently in beta and are subject to
     * change.
     * Allows you to change a provisioned user's individual attributes. To change a user's values, you
     * must provide a specific `Operations` JSON format that contains at least one of the `add`, `remove`,
     *  or `replace` operations. For examples and more information on the SCIM operations format, see the
     * [SCIM specification](https://tools.ietf.org/html/rfc7644#section-3.5.2).
     * **Note:** Complicated SCIM `path` selectors that include filters are not supported. For example, a
     * `path` selector defined as `"path": "emails[type eq \"work\"]"` will not work.
     * **Warning:** If you set `active:false` using the `replace` operation (as shown in the JSON example
     * below), it removes the user from the enterprise, deletes the external identity, and deletes the
     * associated `:scim_user_id`.
     * ```
     * {
     * "Operations":[{
     * "op":"replace",
     * "value":{
     * "active":false
     * }
     * }]
     * }
     * ```.
     */
    abstract public function enterpriseAdminUpdateAttributeForEnterpriseUser(
        EnterpriseAdminUpdateAttributeForEnterpriseUserRequest $request,
        string $enterprise,
        string $scim_user_id,
    ): EnterpriseAdminUpdateAttributeForEnterpriseUserResponse;

    /**
     * DELETE /scim/v2/organizations/{org}/Users/{scim_user_id}
     *
     * Delete a SCIM user from an organization
     */
    abstract public function scimDeleteUserFromOrg(
        string $org,
        string $scim_user_id,
    ): ScimDeleteUserFromOrgResponse;

    /**
     * GET /scim/v2/organizations/{org}/Users/{scim_user_id}
     *
     * Get SCIM provisioning information for a user
     */
    abstract public function scimGetProvisioningInformationForUser(
        string $org,
        string $scim_user_id,
    ): ScimGetProvisioningInformationForUserResponse;

    /**
     * GET /scim/v2/organizations/{org}/Users
     *
     * Retrieves a paginated list of all provisioned organization members, including pending invitations.
     * If you provide the `filter` parameter, the resources for all matching provisions members are
     * returned.
     * When a user with a SAML-provisioned external identity leaves (or is removed from) an organization,
     * the account's metadata is immediately removed. However, the returned list of user accounts might
     * not always match the organization or enterprise member list you see on GitHub. This can happen in
     * certain cases where an external identity associated with an organization will not match an
     * organization member:
     * - When a user with a SCIM-provisioned external identity is removed from an organization, the
     * account's metadata is preserved to allow the user to re-join the organization in the future.
     * - When inviting a user to join an organization, you can expect to see their external identity in
     * the results before they accept the invitation, or if the invitation is cancelled (or never
     * accepted).
     * - When a user is invited over SCIM, an external identity is created that matches with the
     * invitee's email address. However, this identity is only linked to a user account when the user
     * accepts the invitation by going through SAML SSO.
     * The returned list of external identities can include an entry for a `null` user. These are
     * unlinked SAML identities that are created when a user goes through the following Single Sign-On
     * (SSO) process but does not sign in to their GitHub account after completing SSO:
     * 1. The user is granted access by the IdP and is not a member of the GitHub organization.
     * 1. The user attempts to access the GitHub organization and initiates the SAML SSO process, and is
     * not currently signed in to their GitHub account.
     * 1. After successfully authenticating with the SAML SSO IdP, the `null` external identity entry is
     * created and the user is prompted to sign in to their GitHub account:
     * - If the user signs in, their GitHub account is linked to this entry.
     * - If the user does not sign in (or does not create a new account when prompted), they are not
     * added to the GitHub organization, and the external identity `null` entry remains in place.
     */
    abstract public function scimListProvisionedIdentities(
        ScimListProvisionedIdentitiesRequest $request,
        string $org,
    ): ScimListProvisionedIdentitiesResponse;

    /**
     * POST /scim/v2/organizations/{org}/Users
     *
     * Provision organization membership for a user, and send an activation email to the email address.
     */
    abstract public function scimProvisionAndInviteUser(
        ScimProvisionAndInviteUserRequest $request,
        string $org,
    ): ScimProvisionAndInviteUserResponse;

    /**
     * PUT /scim/v2/organizations/{org}/Users/{scim_user_id}
     *
     * Replaces an existing provisioned user's information. You must provide all the information required
     * for the user as if you were provisioning them for the first time. Any existing user information
     * that you don't provide will be removed. If you want to only update a specific attribute, use the
     * [Update an attribute for a SCIM user](https://docs.github.
     * com/rest/reference/scim#update-an-attribute-for-a-scim-user) endpoint instead.
     * You must at least provide the required values for the user: `userName`, `name`, and `emails`.
     * **Warning:** Setting `active: false` removes the user from the organization, deletes the external
     * identity, and deletes the associated `{scim_user_id}`.
     */
    abstract public function scimSetInformationForProvisionedUser(
        ScimSetInformationForProvisionedUserRequest $request,
        string $org,
        string $scim_user_id,
    ): ScimSetInformationForProvisionedUserResponse;

    /**
     * PATCH /scim/v2/organizations/{org}/Users/{scim_user_id}
     *
     * Allows you to change a provisioned user's individual attributes. To change a user's values, you
     * must provide a specific `Operations` JSON format that contains at least one of the `add`, `remove`,
     *  or `replace` operations. For examples and more information on the SCIM operations format, see the
     * [SCIM specification](https://tools.ietf.org/html/rfc7644#section-3.5.2).
     * **Note:** Complicated SCIM `path` selectors that include filters are not supported. For example, a
     * `path` selector defined as `"path": "emails[type eq \"work\"]"` will not work.
     * **Warning:** If you set `active:false` using the `replace` operation (as shown in the JSON example
     * below), it removes the user from the organization, deletes the external identity, and deletes the
     * associated `:scim_user_id`.
     * ```
     * {
     * "Operations":[{
     * "op":"replace",
     * "value":{
     * "active":false
     * }
     * }]
     * }
     * ```.
     */
    abstract public function scimUpdateAttributeForUser(
        ScimUpdateAttributeForUserRequest $request,
        string $org,
        string $scim_user_id,
    ): ScimUpdateAttributeForUserResponse;
}
