<?php

// Code generated by lcodegen, DO NOT EDIT.

namespace App\Http\Controllers\OpenApi;

use App\Http\Controllers\Controller;
use App\Http\Requests\ActionsCreateOrUpdateOrgSecretRequest;
use App\Http\Requests\ActionsCreateSelfHostedRunnerGroupForOrgRequest;
use App\Http\Requests\ActionsListOrgSecretsRequest;
use App\Http\Requests\ActionsListRepoAccessToSelfHostedRunnerGroupInOrgRequest;
use App\Http\Requests\ActionsListSelectedReposForOrgSecretRequest;
use App\Http\Requests\ActionsListSelectedRepositoriesEnabledGithubActionsOrganizationRequest;
use App\Http\Requests\ActionsListSelfHostedRunnerGroupsForOrgRequest;
use App\Http\Requests\ActionsListSelfHostedRunnersForOrgRequest;
use App\Http\Requests\ActionsListSelfHostedRunnersInGroupForOrgRequest;
use App\Http\Requests\ActionsSetAllowedActionsOrganizationRequest;
use App\Http\Requests\ActionsSetGithubActionsPermissionsOrganizationRequest;
use App\Http\Requests\ActionsSetRepoAccessToSelfHostedRunnerGroupInOrgRequest;
use App\Http\Requests\ActionsSetSelectedReposForOrgSecretRequest;
use App\Http\Requests\ActionsSetSelectedRepositoriesEnabledGithubActionsOrganizationRequest;
use App\Http\Requests\ActionsSetSelfHostedRunnersInGroupForOrgRequest;
use App\Http\Requests\ActionsUpdateSelfHostedRunnerGroupForOrgRequest;
use App\Http\Requests\ActivityListPublicOrgEventsRequest;
use App\Http\Requests\InteractionsSetRestrictionsForOrgRequest;
use App\Http\Requests\IssuesListForOrgRequest;
use App\Http\Requests\MigrationsGetStatusForOrgRequest;
use App\Http\Requests\MigrationsListForOrgRequest;
use App\Http\Requests\MigrationsListReposForOrgRequest;
use App\Http\Requests\MigrationsStartForOrgRequest;
use App\Http\Requests\OrgsCreateInvitationRequest;
use App\Http\Requests\OrgsCreateWebhookRequest;
use App\Http\Requests\OrgsGetAuditLogRequest;
use App\Http\Requests\OrgsListFailedInvitationsRequest;
use App\Http\Requests\OrgsListInvitationTeamsRequest;
use App\Http\Requests\OrgsListMembersRequest;
use App\Http\Requests\OrgsListOutsideCollaboratorsRequest;
use App\Http\Requests\OrgsListPendingInvitationsRequest;
use App\Http\Requests\OrgsListPublicMembersRequest;
use App\Http\Requests\OrgsListWebhookDeliveriesRequest;
use App\Http\Requests\OrgsListWebhooksRequest;
use App\Http\Requests\OrgsSetMembershipForUserRequest;
use App\Http\Requests\OrgsUpdateWebhookRequest;
use App\Http\Requests\OrgsUpdateWebhookConfigForOrgRequest;
use App\Http\Requests\PackagesGetAllPackageVersionsForPackageOwnedByOrgRequest;
use App\Http\Requests\PackagesListPackagesForOrganizationRequest;
use App\Http\Requests\PackagesRestorePackageForOrgRequest;
use App\Http\Requests\ProjectsCreateForOrgRequest;
use App\Http\Requests\ProjectsListForOrgRequest;
use App\Http\Requests\ReactionsCreateForTeamDiscussionCommentInOrgRequest;
use App\Http\Requests\ReactionsCreateForTeamDiscussionInOrgRequest;
use App\Http\Requests\ReactionsListForTeamDiscussionCommentInOrgRequest;
use App\Http\Requests\ReactionsListForTeamDiscussionInOrgRequest;
use App\Http\Requests\ReposCreateInOrgRequest;
use App\Http\Requests\ReposListForOrgRequest;
use App\Http\Requests\SecretScanningListAlertsForOrgRequest;
use App\Http\Requests\TeamsAddOrUpdateMembershipForUserInOrgRequest;
use App\Http\Requests\TeamsAddOrUpdateProjectPermissionsInOrgRequest;
use App\Http\Requests\TeamsAddOrUpdateRepoPermissionsInOrgRequest;
use App\Http\Requests\TeamsCreateRequest;
use App\Http\Requests\TeamsCreateDiscussionCommentInOrgRequest;
use App\Http\Requests\TeamsCreateDiscussionInOrgRequest;
use App\Http\Requests\TeamsCreateOrUpdateIdpGroupConnectionsInOrgRequest;
use App\Http\Requests\TeamsListRequest;
use App\Http\Requests\TeamsListChildInOrgRequest;
use App\Http\Requests\TeamsListDiscussionCommentsInOrgRequest;
use App\Http\Requests\TeamsListDiscussionsInOrgRequest;
use App\Http\Requests\TeamsListIdpGroupsForOrgRequest;
use App\Http\Requests\TeamsListMembersInOrgRequest;
use App\Http\Requests\TeamsListPendingInvitationsInOrgRequest;
use App\Http\Requests\TeamsListProjectsInOrgRequest;
use App\Http\Requests\TeamsListReposInOrgRequest;
use App\Http\Requests\TeamsUpdateDiscussionCommentInOrgRequest;
use App\Http\Requests\TeamsUpdateDiscussionInOrgRequest;
use App\Http\Requests\TeamsUpdateInOrgRequest;
use App\Http\Responses\OpenApi\ActionsAddRepoAccessToSelfHostedRunnerGroupInOrgResponse;
use App\Http\Responses\OpenApi\ActionsAddSelectedRepoToOrgSecretResponse;
use App\Http\Responses\OpenApi\ActionsAddSelfHostedRunnerToGroupForOrgResponse;
use App\Http\Responses\OpenApi\ActionsCreateOrUpdateOrgSecretResponse;
use App\Http\Responses\OpenApi\ActionsCreateRegistrationTokenForOrgResponse;
use App\Http\Responses\OpenApi\ActionsCreateRemoveTokenForOrgResponse;
use App\Http\Responses\OpenApi\ActionsCreateSelfHostedRunnerGroupForOrgResponse;
use App\Http\Responses\OpenApi\ActionsDeleteOrgSecretResponse;
use App\Http\Responses\OpenApi\ActionsDeleteSelfHostedRunnerFromOrgResponse;
use App\Http\Responses\OpenApi\ActionsDeleteSelfHostedRunnerGroupFromOrgResponse;
use App\Http\Responses\OpenApi\ActionsDisableSelectedRepositoryGithubActionsOrganizationResponse;
use App\Http\Responses\OpenApi\ActionsEnableSelectedRepositoryGithubActionsOrganizationResponse;
use App\Http\Responses\OpenApi\ActionsGetAllowedActionsOrganizationResponse;
use App\Http\Responses\OpenApi\ActionsGetGithubActionsPermissionsOrganizationResponse;
use App\Http\Responses\OpenApi\ActionsGetOrgPublicKeyResponse;
use App\Http\Responses\OpenApi\ActionsGetOrgSecretResponse;
use App\Http\Responses\OpenApi\ActionsGetSelfHostedRunnerForOrgResponse;
use App\Http\Responses\OpenApi\ActionsGetSelfHostedRunnerGroupForOrgResponse;
use App\Http\Responses\OpenApi\ActionsListOrgSecretsResponse;
use App\Http\Responses\OpenApi\ActionsListRepoAccessToSelfHostedRunnerGroupInOrgResponse;
use App\Http\Responses\OpenApi\ActionsListRunnerApplicationsForOrgResponse;
use App\Http\Responses\OpenApi\ActionsListSelectedReposForOrgSecretResponse;
use App\Http\Responses\OpenApi\ActionsListSelectedRepositoriesEnabledGithubActionsOrganizationResponse;
use App\Http\Responses\OpenApi\ActionsListSelfHostedRunnerGroupsForOrgResponse;
use App\Http\Responses\OpenApi\ActionsListSelfHostedRunnersForOrgResponse;
use App\Http\Responses\OpenApi\ActionsListSelfHostedRunnersInGroupForOrgResponse;
use App\Http\Responses\OpenApi\ActionsRemoveRepoAccessToSelfHostedRunnerGroupInOrgResponse;
use App\Http\Responses\OpenApi\ActionsRemoveSelectedRepoFromOrgSecretResponse;
use App\Http\Responses\OpenApi\ActionsRemoveSelfHostedRunnerFromGroupForOrgResponse;
use App\Http\Responses\OpenApi\ActionsSetAllowedActionsOrganizationResponse;
use App\Http\Responses\OpenApi\ActionsSetGithubActionsPermissionsOrganizationResponse;
use App\Http\Responses\OpenApi\ActionsSetRepoAccessToSelfHostedRunnerGroupInOrgResponse;
use App\Http\Responses\OpenApi\ActionsSetSelectedReposForOrgSecretResponse;
use App\Http\Responses\OpenApi\ActionsSetSelectedRepositoriesEnabledGithubActionsOrganizationResponse;
use App\Http\Responses\OpenApi\ActionsSetSelfHostedRunnersInGroupForOrgResponse;
use App\Http\Responses\OpenApi\ActionsUpdateSelfHostedRunnerGroupForOrgResponse;
use App\Http\Responses\OpenApi\ActivityListPublicOrgEventsResponse;
use App\Http\Responses\OpenApi\BillingGetGithubActionsBillingOrgResponse;
use App\Http\Responses\OpenApi\BillingGetGithubPackagesBillingOrgResponse;
use App\Http\Responses\OpenApi\BillingGetSharedStorageBillingOrgResponse;
use App\Http\Responses\OpenApi\InteractionsRemoveRestrictionsForOrgResponse;
use App\Http\Responses\OpenApi\InteractionsSetRestrictionsForOrgResponse;
use App\Http\Responses\OpenApi\IssuesListForOrgResponse;
use App\Http\Responses\OpenApi\MigrationsDeleteArchiveForOrgResponse;
use App\Http\Responses\OpenApi\MigrationsGetStatusForOrgResponse;
use App\Http\Responses\OpenApi\MigrationsListForOrgResponse;
use App\Http\Responses\OpenApi\MigrationsListReposForOrgResponse;
use App\Http\Responses\OpenApi\MigrationsStartForOrgResponse;
use App\Http\Responses\OpenApi\MigrationsUnlockRepoForOrgResponse;
use App\Http\Responses\OpenApi\OrgsBlockUserResponse;
use App\Http\Responses\OpenApi\OrgsCancelInvitationResponse;
use App\Http\Responses\OpenApi\OrgsCheckBlockedUserResponse;
use App\Http\Responses\OpenApi\OrgsCheckMembershipForUserResponse;
use App\Http\Responses\OpenApi\OrgsCheckPublicMembershipForUserResponse;
use App\Http\Responses\OpenApi\OrgsConvertMemberToOutsideCollaboratorResponse;
use App\Http\Responses\OpenApi\OrgsCreateInvitationResponse;
use App\Http\Responses\OpenApi\OrgsCreateWebhookResponse;
use App\Http\Responses\OpenApi\OrgsDeleteWebhookResponse;
use App\Http\Responses\OpenApi\OrgsGetAuditLogResponse;
use App\Http\Responses\OpenApi\OrgsGetMembershipForUserResponse;
use App\Http\Responses\OpenApi\OrgsGetResponse;
use App\Http\Responses\OpenApi\OrgsGetWebhookConfigForOrgResponse;
use App\Http\Responses\OpenApi\OrgsGetWebhookDeliveryResponse;
use App\Http\Responses\OpenApi\OrgsGetWebhookResponse;
use App\Http\Responses\OpenApi\OrgsListBlockedUsersResponse;
use App\Http\Responses\OpenApi\OrgsListFailedInvitationsResponse;
use App\Http\Responses\OpenApi\OrgsListInvitationTeamsResponse;
use App\Http\Responses\OpenApi\OrgsListMembersResponse;
use App\Http\Responses\OpenApi\OrgsListOutsideCollaboratorsResponse;
use App\Http\Responses\OpenApi\OrgsListPendingInvitationsResponse;
use App\Http\Responses\OpenApi\OrgsListPublicMembersResponse;
use App\Http\Responses\OpenApi\OrgsListSamlSsoAuthorizationsResponse;
use App\Http\Responses\OpenApi\OrgsListWebhookDeliveriesResponse;
use App\Http\Responses\OpenApi\OrgsListWebhooksResponse;
use App\Http\Responses\OpenApi\OrgsPingWebhookResponse;
use App\Http\Responses\OpenApi\OrgsRedeliverWebhookDeliveryResponse;
use App\Http\Responses\OpenApi\OrgsRemoveMemberResponse;
use App\Http\Responses\OpenApi\OrgsRemoveMembershipForUserResponse;
use App\Http\Responses\OpenApi\OrgsRemoveOutsideCollaboratorResponse;
use App\Http\Responses\OpenApi\OrgsRemovePublicMembershipForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\OrgsRemoveSamlSsoAuthorizationResponse;
use App\Http\Responses\OpenApi\OrgsSetMembershipForUserResponse;
use App\Http\Responses\OpenApi\OrgsSetPublicMembershipForAuthenticatedUserResponse;
use App\Http\Responses\OpenApi\OrgsUnblockUserResponse;
use App\Http\Responses\OpenApi\OrgsUpdateWebhookConfigForOrgResponse;
use App\Http\Responses\OpenApi\OrgsUpdateWebhookResponse;
use App\Http\Responses\OpenApi\PackagesDeletePackageForOrgResponse;
use App\Http\Responses\OpenApi\PackagesDeletePackageVersionForOrgResponse;
use App\Http\Responses\OpenApi\PackagesGetAllPackageVersionsForPackageOwnedByOrgResponse;
use App\Http\Responses\OpenApi\PackagesGetPackageForOrganizationResponse;
use App\Http\Responses\OpenApi\PackagesGetPackageVersionForOrganizationResponse;
use App\Http\Responses\OpenApi\PackagesListPackagesForOrganizationResponse;
use App\Http\Responses\OpenApi\PackagesRestorePackageForOrgResponse;
use App\Http\Responses\OpenApi\PackagesRestorePackageVersionForOrgResponse;
use App\Http\Responses\OpenApi\ProjectsCreateForOrgResponse;
use App\Http\Responses\OpenApi\ProjectsListForOrgResponse;
use App\Http\Responses\OpenApi\ReactionsCreateForTeamDiscussionCommentInOrgResponse;
use App\Http\Responses\OpenApi\ReactionsCreateForTeamDiscussionInOrgResponse;
use App\Http\Responses\OpenApi\ReactionsDeleteForTeamDiscussionCommentResponse;
use App\Http\Responses\OpenApi\ReactionsDeleteForTeamDiscussionResponse;
use App\Http\Responses\OpenApi\ReactionsListForTeamDiscussionCommentInOrgResponse;
use App\Http\Responses\OpenApi\ReactionsListForTeamDiscussionInOrgResponse;
use App\Http\Responses\OpenApi\ReposCreateInOrgResponse;
use App\Http\Responses\OpenApi\ReposListForOrgResponse;
use App\Http\Responses\OpenApi\SecretScanningListAlertsForOrgResponse;
use App\Http\Responses\OpenApi\TeamsAddOrUpdateMembershipForUserInOrgResponse;
use App\Http\Responses\OpenApi\TeamsAddOrUpdateProjectPermissionsInOrgResponse;
use App\Http\Responses\OpenApi\TeamsAddOrUpdateRepoPermissionsInOrgResponse;
use App\Http\Responses\OpenApi\TeamsCheckPermissionsForProjectInOrgResponse;
use App\Http\Responses\OpenApi\TeamsCheckPermissionsForRepoInOrgResponse;
use App\Http\Responses\OpenApi\TeamsCreateDiscussionCommentInOrgResponse;
use App\Http\Responses\OpenApi\TeamsCreateDiscussionInOrgResponse;
use App\Http\Responses\OpenApi\TeamsCreateOrUpdateIdpGroupConnectionsInOrgResponse;
use App\Http\Responses\OpenApi\TeamsCreateResponse;
use App\Http\Responses\OpenApi\TeamsDeleteDiscussionCommentInOrgResponse;
use App\Http\Responses\OpenApi\TeamsDeleteDiscussionInOrgResponse;
use App\Http\Responses\OpenApi\TeamsDeleteInOrgResponse;
use App\Http\Responses\OpenApi\TeamsGetByNameResponse;
use App\Http\Responses\OpenApi\TeamsGetDiscussionCommentInOrgResponse;
use App\Http\Responses\OpenApi\TeamsGetDiscussionInOrgResponse;
use App\Http\Responses\OpenApi\TeamsGetMembershipForUserInOrgResponse;
use App\Http\Responses\OpenApi\TeamsListChildInOrgResponse;
use App\Http\Responses\OpenApi\TeamsListDiscussionCommentsInOrgResponse;
use App\Http\Responses\OpenApi\TeamsListDiscussionsInOrgResponse;
use App\Http\Responses\OpenApi\TeamsListIdpGroupsForOrgResponse;
use App\Http\Responses\OpenApi\TeamsListIdpGroupsInOrgResponse;
use App\Http\Responses\OpenApi\TeamsListMembersInOrgResponse;
use App\Http\Responses\OpenApi\TeamsListPendingInvitationsInOrgResponse;
use App\Http\Responses\OpenApi\TeamsListProjectsInOrgResponse;
use App\Http\Responses\OpenApi\TeamsListReposInOrgResponse;
use App\Http\Responses\OpenApi\TeamsListResponse;
use App\Http\Responses\OpenApi\TeamsRemoveMembershipForUserInOrgResponse;
use App\Http\Responses\OpenApi\TeamsRemoveProjectInOrgResponse;
use App\Http\Responses\OpenApi\TeamsRemoveRepoInOrgResponse;
use App\Http\Responses\OpenApi\TeamsUpdateDiscussionCommentInOrgResponse;
use App\Http\Responses\OpenApi\TeamsUpdateDiscussionInOrgResponse;
use App\Http\Responses\OpenApi\TeamsUpdateInOrgResponse;

/**
 * Auto-generated abstract controller from OpenAPI specification.
 * Extend this class and implement the abstract methods.
 */
abstract class OrgController extends Controller
{
    /**
     * PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories/{repository_id}
     *
     * The self-hosted runner groups REST API is available with GitHub Enterprise Cloud. For more
     * information, see "[GitHub's products](https://docs.github.
     * com/github/getting-started-with-github/githubs-products)."
     * Adds a repository to the list of selected repositories that can access a self-hosted runner group.
     * The runner group must have `visibility` set to `selected`. For more information, see "[Create a
     * self-hosted runner group for an
     * organization](#create-a-self-hosted-runner-group-for-an-organization)."
     * You must authenticate using an access token with the `admin:org`
     * scope to use this endpoint.
     */
    abstract public function actionsAddRepoAccessToSelfHostedRunnerGroupInOrg(
        string $org,
        int $runner_group_id,
        int $repository_id,
    ): ActionsAddRepoAccessToSelfHostedRunnerGroupInOrgResponse;

    /**
     * PUT /orgs/{org}/actions/secrets/{secret_name}/repositories/{repository_id}
     *
     * Adds a repository to an organization secret when the `visibility` for repository access is set to
     * `selected`. The visibility is set when you [Create or update an organization secret](https://docs.
     * github.com/rest/reference/actions#create-or-update-an-organization-secret). You must authenticate
     * using an access token with the `admin:org` scope to use this endpoint. GitHub Apps must have the
     * `secrets` organization permission to use this endpoint.
     */
    abstract public function actionsAddSelectedRepoToOrgSecret(
        string $org,
        string $secret_name,
        int $repository_id,
    ): ActionsAddSelectedRepoToOrgSecretResponse;

    /**
     * PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/runners/{runner_id}
     *
     * The self-hosted runner groups REST API is available with GitHub Enterprise Cloud. For more
     * information, see "[GitHub's products](https://docs.github.
     * com/github/getting-started-with-github/githubs-products)."
     * Adds a self-hosted runner to a runner group configured in an organization.
     * You must authenticate using an access token with the `admin:org`
     * scope to use this endpoint.
     */
    abstract public function actionsAddSelfHostedRunnerToGroupForOrg(
        string $org,
        int $runner_group_id,
        int $runner_id,
    ): ActionsAddSelfHostedRunnerToGroupForOrgResponse;

    /**
     * PUT /orgs/{org}/actions/secrets/{secret_name}
     *
     * Creates or updates an organization secret with an encrypted value. Encrypt your secret using
     * [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages). You must authenticate
     * using an access
     * token with the `admin:org` scope to use this endpoint. GitHub Apps must have the `secrets`
     * organization permission to
     * use this endpoint.
     * #### Example encrypting a secret using Node.js
     * Encrypt your secret using the [tweetsodium](https://github.com/github/tweetsodium) library.
     * ```
     * const sodium = require('tweetsodium');
     * const key = "base64-encoded-public-key";
     * const value = "plain-text-secret";
     * // Convert the message and key to Uint8Array's (Buffer implements that interface)
     * const messageBytes = Buffer.from(value);
     * const keyBytes = Buffer.from(key, 'base64');
     * // Encrypt using LibSodium.
     * const encryptedBytes = sodium.seal(messageBytes, keyBytes);
     * // Base64 the encrypted secret
     * const encrypted = Buffer.from(encryptedBytes).toString('base64');
     * console.log(encrypted);
     * ```
     * #### Example encrypting a secret using Python
     * Encrypt your secret using [pynacl](https://pynacl.readthedocs.
     * io/en/stable/public/#nacl-public-sealedbox) with Python 3.
     * ```
     * from base64 import b64encode
     * from nacl import encoding, public
     * def encrypt(public_key: str, secret_value: str) -> str:
     * """Encrypt a Unicode string using the public key."""
     * public_key = public.PublicKey(public_key.encode("utf-8"), encoding.Base64Encoder())
     * sealed_box = public.SealedBox(public_key)
     * encrypted = sealed_box.encrypt(secret_value.encode("utf-8"))
     * return b64encode(encrypted).decode("utf-8")
     * ```
     * #### Example encrypting a secret using C#
     * Encrypt your secret using the [Sodium.Core](https://www.nuget.org/packages/Sodium.Core/) package.
     * ```
     * var secretValue = System.Text.Encoding.UTF8.GetBytes("mySecret");
     * var publicKey = Convert.FromBase64String("2Sg8iYjAxxmI2LvUXpJjkYrMxURPc8r+dB7TJyvvcCU=");
     * var sealedPublicKeyBox = Sodium.SealedPublicKeyBox.Create(secretValue, publicKey);
     * Console.WriteLine(Convert.ToBase64String(sealedPublicKeyBox));
     * ```
     * #### Example encrypting a secret using Ruby
     * Encrypt your secret using the [rbnacl](https://github.com/RubyCrypto/rbnacl) gem.
     * ```ruby
     * require "rbnacl"
     * require "base64"
     * key = Base64.decode64("+ZYvJDZMHUfBkJdyq5Zm9SKqeuBQ4sj+6sfjlH4CgG0=")
     * public_key = RbNaCl::PublicKey.new(key)
     * box = RbNaCl::Boxes::Sealed.from_public_key(public_key)
     * encrypted_secret = box.encrypt("my_secret")
     * # Print the base64 encoded secret
     * puts Base64.strict_encode64(encrypted_secret)
     * ```.
     */
    abstract public function actionsCreateOrUpdateOrgSecret(
        ActionsCreateOrUpdateOrgSecretRequest $request,
        string $org,
        string $secret_name,
    ): ActionsCreateOrUpdateOrgSecretResponse;

    /**
     * POST /orgs/{org}/actions/runners/registration-token
     *
     * Returns a token that you can pass to the `config` script. The token expires after one hour.
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     * #### Example using registration token
     * Configure your self-hosted runner, replacing `TOKEN` with the registration token provided by this
     * endpoint.
     * ```
     * ./config.sh --url https://github.com/octo-org --token TOKEN
     * ```.
     */
    abstract public function actionsCreateRegistrationTokenForOrg(
        string $org,
    ): ActionsCreateRegistrationTokenForOrgResponse;

    /**
     * POST /orgs/{org}/actions/runners/remove-token
     *
     * Returns a token that you can pass to the `config` script to remove a self-hosted runner from an
     * organization. The token expires after one hour.
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     * #### Example using remove token
     * To remove your self-hosted runner from an organization, replace `TOKEN` with the remove token
     * provided by this
     * endpoint.
     * ```
     * ./config.sh remove --token TOKEN
     * ```.
     */
    abstract public function actionsCreateRemoveTokenForOrg(
        string $org,
    ): ActionsCreateRemoveTokenForOrgResponse;

    /**
     * POST /orgs/{org}/actions/runner-groups
     *
     * The self-hosted runner groups REST API is available with GitHub Enterprise Cloud and GitHub
     * Enterprise Server. For more information, see "[GitHub's products](https://docs.github.
     * com/github/getting-started-with-github/githubs-products)."
     * Creates a new self-hosted runner group for an organization.
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     */
    abstract public function actionsCreateSelfHostedRunnerGroupForOrg(
        ActionsCreateSelfHostedRunnerGroupForOrgRequest $request,
        string $org,
    ): ActionsCreateSelfHostedRunnerGroupForOrgResponse;

    /**
     * DELETE /orgs/{org}/actions/secrets/{secret_name}
     *
     * Deletes a secret in an organization using the secret name. You must authenticate using an access
     * token with the `admin:org` scope to use this endpoint. GitHub Apps must have the `secrets`
     * organization permission to use this endpoint.
     */
    abstract public function actionsDeleteOrgSecret(
        string $org,
        string $secret_name,
    ): ActionsDeleteOrgSecretResponse;

    /**
     * DELETE /orgs/{org}/actions/runners/{runner_id}
     *
     * Forces the removal of a self-hosted runner from an organization. You can use this endpoint to
     * completely remove the runner when the machine you were using no longer exists.
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     */
    abstract public function actionsDeleteSelfHostedRunnerFromOrg(
        string $org,
        int $runner_id,
    ): ActionsDeleteSelfHostedRunnerFromOrgResponse;

    /**
     * DELETE /orgs/{org}/actions/runner-groups/{runner_group_id}
     *
     * The self-hosted runner groups REST API is available with GitHub Enterprise Cloud. For more
     * information, see "[GitHub's products](https://docs.github.
     * com/github/getting-started-with-github/githubs-products)."
     * Deletes a self-hosted runner group for an organization.
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     */
    abstract public function actionsDeleteSelfHostedRunnerGroupFromOrg(
        string $org,
        int $runner_group_id,
    ): ActionsDeleteSelfHostedRunnerGroupFromOrgResponse;

    /**
     * DELETE /orgs/{org}/actions/permissions/repositories/{repository_id}
     *
     * Removes a repository from the list of selected repositories that are enabled for GitHub Actions in
     * an organization. To use this endpoint, the organization permission policy for
     * `enabled_repositories` must be configured to `selected`. For more information, see "[Set GitHub
     * Actions permissions for an organization](#set-github-actions-permissions-for-an-organization)."
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     * GitHub Apps must have the `administration` organization permission to use this API.
     */
    abstract public function actionsDisableSelectedRepositoryGithubActionsOrganization(
        string $org,
        int $repository_id,
    ): ActionsDisableSelectedRepositoryGithubActionsOrganizationResponse;

    /**
     * PUT /orgs/{org}/actions/permissions/repositories/{repository_id}
     *
     * Adds a repository to the list of selected repositories that are enabled for GitHub Actions in an
     * organization. To use this endpoint, the organization permission policy for `enabled_repositories`
     * must be must be configured to `selected`. For more information, see "[Set GitHub Actions
     * permissions for an organization](#set-github-actions-permissions-for-an-organization)."
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     * GitHub Apps must have the `administration` organization permission to use this API.
     */
    abstract public function actionsEnableSelectedRepositoryGithubActionsOrganization(
        string $org,
        int $repository_id,
    ): ActionsEnableSelectedRepositoryGithubActionsOrganizationResponse;

    /**
     * GET /orgs/{org}/actions/permissions/selected-actions
     *
     * Gets the selected actions that are allowed in an organization. To use this endpoint, the
     * organization permission policy for `allowed_actions` must be configured to `selected`. For more
     * information, see "[Set GitHub Actions permissions for an
     * organization](#set-github-actions-permissions-for-an-organization).""
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     * GitHub Apps must have the `administration` organization permission to use this API.
     */
    abstract public function actionsGetAllowedActionsOrganization(
        string $org,
    ): ActionsGetAllowedActionsOrganizationResponse;

    /**
     * GET /orgs/{org}/actions/permissions
     *
     * Gets the GitHub Actions permissions policy for repositories and allowed actions in an organization.
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     * GitHub Apps must have the `administration` organization permission to use this API.
     */
    abstract public function actionsGetGithubActionsPermissionsOrganization(
        string $org,
    ): ActionsGetGithubActionsPermissionsOrganizationResponse;

    /**
     * GET /orgs/{org}/actions/secrets/public-key
     *
     * Gets your public key, which you need to encrypt secrets. You need to encrypt a secret before you
     * can create or update secrets. You must authenticate using an access token with the `admin:org`
     * scope to use this endpoint. GitHub Apps must have the `secrets` organization permission to use
     * this endpoint.
     */
    abstract public function actionsGetOrgPublicKey(
        string $org,
    ): ActionsGetOrgPublicKeyResponse;

    /**
     * GET /orgs/{org}/actions/secrets/{secret_name}
     *
     * Gets a single organization secret without revealing its encrypted value. You must authenticate
     * using an access token with the `admin:org` scope to use this endpoint. GitHub Apps must have the
     * `secrets` organization permission to use this endpoint.
     */
    abstract public function actionsGetOrgSecret(
        string $org,
        string $secret_name,
    ): ActionsGetOrgSecretResponse;

    /**
     * GET /orgs/{org}/actions/runners/{runner_id}
     *
     * Gets a specific self-hosted runner configured in an organization.
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     */
    abstract public function actionsGetSelfHostedRunnerForOrg(
        string $org,
        int $runner_id,
    ): ActionsGetSelfHostedRunnerForOrgResponse;

    /**
     * GET /orgs/{org}/actions/runner-groups/{runner_group_id}
     *
     * The self-hosted runner groups REST API is available with GitHub Enterprise Cloud. For more
     * information, see "[GitHub's products](https://docs.github.
     * com/github/getting-started-with-github/githubs-products)."
     * Gets a specific self-hosted runner group for an organization.
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     */
    abstract public function actionsGetSelfHostedRunnerGroupForOrg(
        string $org,
        int $runner_group_id,
    ): ActionsGetSelfHostedRunnerGroupForOrgResponse;

    /**
     * GET /orgs/{org}/actions/secrets
     *
     * Lists all secrets available in an organization without revealing their encrypted values. You must
     * authenticate using an access token with the `admin:org` scope to use this endpoint. GitHub Apps
     * must have the `secrets` organization permission to use this endpoint.
     */
    abstract public function actionsListOrgSecrets(
        ActionsListOrgSecretsRequest $request,
        string $org,
    ): ActionsListOrgSecretsResponse;

    /**
     * GET /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories
     *
     * The self-hosted runner groups REST API is available with GitHub Enterprise Cloud and GitHub
     * Enterprise Server. For more information, see "[GitHub's products](https://docs.github.
     * com/github/getting-started-with-github/githubs-products)."
     * Lists the repositories with access to a self-hosted runner group configured in an organization.
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     */
    abstract public function actionsListRepoAccessToSelfHostedRunnerGroupInOrg(
        ActionsListRepoAccessToSelfHostedRunnerGroupInOrgRequest $request,
        string $org,
        int $runner_group_id,
    ): ActionsListRepoAccessToSelfHostedRunnerGroupInOrgResponse;

    /**
     * GET /orgs/{org}/actions/runners/downloads
     *
     * Lists binaries for the runner application that you can download and run.
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     */
    abstract public function actionsListRunnerApplicationsForOrg(
        string $org,
    ): ActionsListRunnerApplicationsForOrgResponse;

    /**
     * GET /orgs/{org}/actions/secrets/{secret_name}/repositories
     *
     * Lists all repositories that have been selected when the `visibility` for repository access to a
     * secret is set to `selected`. You must authenticate using an access token with the `admin:org`
     * scope to use this endpoint. GitHub Apps must have the `secrets` organization permission to use
     * this endpoint.
     */
    abstract public function actionsListSelectedReposForOrgSecret(
        ActionsListSelectedReposForOrgSecretRequest $request,
        string $org,
        string $secret_name,
    ): ActionsListSelectedReposForOrgSecretResponse;

    /**
     * GET /orgs/{org}/actions/permissions/repositories
     *
     * Lists the selected repositories that are enabled for GitHub Actions in an organization. To use
     * this endpoint, the organization permission policy for `enabled_repositories` must be configured to
     * `selected`. For more information, see "[Set GitHub Actions permissions for an
     * organization](#set-github-actions-permissions-for-an-organization)."
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     * GitHub Apps must have the `administration` organization permission to use this API.
     */
    abstract public function actionsListSelectedRepositoriesEnabledGithubActionsOrganization(
        ActionsListSelectedRepositoriesEnabledGithubActionsOrganizationRequest $request,
        string $org,
    ): ActionsListSelectedRepositoriesEnabledGithubActionsOrganizationResponse;

    /**
     * GET /orgs/{org}/actions/runner-groups
     *
     * The self-hosted runner groups REST API is available with GitHub Enterprise Cloud. For more
     * information, see "[GitHub's products](https://docs.github.
     * com/github/getting-started-with-github/githubs-products)."
     * Lists all self-hosted runner groups configured in an organization and inherited from an enterprise.
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     */
    abstract public function actionsListSelfHostedRunnerGroupsForOrg(
        ActionsListSelfHostedRunnerGroupsForOrgRequest $request,
        string $org,
    ): ActionsListSelfHostedRunnerGroupsForOrgResponse;

    /**
     * GET /orgs/{org}/actions/runners
     *
     * Lists all self-hosted runners configured in an organization.
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     */
    abstract public function actionsListSelfHostedRunnersForOrg(
        ActionsListSelfHostedRunnersForOrgRequest $request,
        string $org,
    ): ActionsListSelfHostedRunnersForOrgResponse;

    /**
     * GET /orgs/{org}/actions/runner-groups/{runner_group_id}/runners
     *
     * The self-hosted runner groups REST API is available with GitHub Enterprise Cloud. For more
     * information, see "[GitHub's products](https://docs.github.
     * com/github/getting-started-with-github/githubs-products)."
     * Lists self-hosted runners that are in a specific organization group.
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     */
    abstract public function actionsListSelfHostedRunnersInGroupForOrg(
        ActionsListSelfHostedRunnersInGroupForOrgRequest $request,
        string $org,
        int $runner_group_id,
    ): ActionsListSelfHostedRunnersInGroupForOrgResponse;

    /**
     * DELETE /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories/{repository_id}
     *
     * The self-hosted runner groups REST API is available with GitHub Enterprise Cloud. For more
     * information, see "[GitHub's products](https://docs.github.
     * com/github/getting-started-with-github/githubs-products)."
     * Removes a repository from the list of selected repositories that can access a self-hosted runner
     * group. The runner group must have `visibility` set to `selected`. For more information, see
     * "[Create a self-hosted runner group for an
     * organization](#create-a-self-hosted-runner-group-for-an-organization)."
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     */
    abstract public function actionsRemoveRepoAccessToSelfHostedRunnerGroupInOrg(
        string $org,
        int $runner_group_id,
        int $repository_id,
    ): ActionsRemoveRepoAccessToSelfHostedRunnerGroupInOrgResponse;

    /**
     * DELETE /orgs/{org}/actions/secrets/{secret_name}/repositories/{repository_id}
     *
     * Removes a repository from an organization secret when the `visibility` for repository access is
     * set to `selected`. The visibility is set when you [Create or update an organization
     * secret](https://docs.github.com/rest/reference/actions#create-or-update-an-organization-secret).
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     * GitHub Apps must have the `secrets` organization permission to use this endpoint.
     */
    abstract public function actionsRemoveSelectedRepoFromOrgSecret(
        string $org,
        string $secret_name,
        int $repository_id,
    ): ActionsRemoveSelectedRepoFromOrgSecretResponse;

    /**
     * DELETE /orgs/{org}/actions/runner-groups/{runner_group_id}/runners/{runner_id}
     *
     * The self-hosted runner groups REST API is available with GitHub Enterprise Cloud. For more
     * information, see "[GitHub's products](https://docs.github.
     * com/github/getting-started-with-github/githubs-products)."
     * Removes a self-hosted runner from a group configured in an organization. The runner is then
     * returned to the default group.
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     */
    abstract public function actionsRemoveSelfHostedRunnerFromGroupForOrg(
        string $org,
        int $runner_group_id,
        int $runner_id,
    ): ActionsRemoveSelfHostedRunnerFromGroupForOrgResponse;

    /**
     * PUT /orgs/{org}/actions/permissions/selected-actions
     *
     * Sets the actions that are allowed in an organization. To use this endpoint, the organization
     * permission policy for `allowed_actions` must be configured to `selected`. For more information,
     * see "[Set GitHub Actions permissions for an
     * organization](#set-github-actions-permissions-for-an-organization)."
     * If the organization belongs to an enterprise that has `selected` actions set at the enterprise
     * level, then you cannot override any of the enterprise's allowed actions settings.
     * To use the `patterns_allowed` setting for private repositories, the organization must belong to an
     * enterprise. If the organization does not belong to an enterprise, then the `patterns_allowed`
     * setting only applies to public repositories in the organization.
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     * GitHub Apps must have the `administration` organization permission to use this API.
     */
    abstract public function actionsSetAllowedActionsOrganization(
        ActionsSetAllowedActionsOrganizationRequest $request,
        string $org,
    ): ActionsSetAllowedActionsOrganizationResponse;

    /**
     * PUT /orgs/{org}/actions/permissions
     *
     * Sets the GitHub Actions permissions policy for repositories and allowed actions in an organization.
     * If the organization belongs to an enterprise that has set restrictive permissions at the
     * enterprise level, such as `allowed_actions` to `selected` actions, then you cannot override them
     * for the organization.
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     * GitHub Apps must have the `administration` organization permission to use this API.
     */
    abstract public function actionsSetGithubActionsPermissionsOrganization(
        ActionsSetGithubActionsPermissionsOrganizationRequest $request,
        string $org,
    ): ActionsSetGithubActionsPermissionsOrganizationResponse;

    /**
     * PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories
     *
     * The self-hosted runner groups REST API is available with GitHub Enterprise Cloud. For more
     * information, see "[GitHub's products](https://docs.github.
     * com/github/getting-started-with-github/githubs-products)."
     * Replaces the list of repositories that have access to a self-hosted runner group configured in an
     * organization.
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     */
    abstract public function actionsSetRepoAccessToSelfHostedRunnerGroupInOrg(
        ActionsSetRepoAccessToSelfHostedRunnerGroupInOrgRequest $request,
        string $org,
        int $runner_group_id,
    ): ActionsSetRepoAccessToSelfHostedRunnerGroupInOrgResponse;

    /**
     * PUT /orgs/{org}/actions/secrets/{secret_name}/repositories
     *
     * Replaces all repositories for an organization secret when the `visibility` for repository access
     * is set to `selected`. The visibility is set when you [Create or update an organization
     * secret](https://docs.github.com/rest/reference/actions#create-or-update-an-organization-secret).
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     * GitHub Apps must have the `secrets` organization permission to use this endpoint.
     */
    abstract public function actionsSetSelectedReposForOrgSecret(
        ActionsSetSelectedReposForOrgSecretRequest $request,
        string $org,
        string $secret_name,
    ): ActionsSetSelectedReposForOrgSecretResponse;

    /**
     * PUT /orgs/{org}/actions/permissions/repositories
     *
     * Replaces the list of selected repositories that are enabled for GitHub Actions in an organization.
     * To use this endpoint, the organization permission policy for `enabled_repositories` must be
     * configured to `selected`. For more information, see "[Set GitHub Actions permissions for an
     * organization](#set-github-actions-permissions-for-an-organization)."
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     * GitHub Apps must have the `administration` organization permission to use this API.
     */
    abstract public function actionsSetSelectedRepositoriesEnabledGithubActionsOrganization(
        ActionsSetSelectedRepositoriesEnabledGithubActionsOrganizationRequest $request,
        string $org,
    ): ActionsSetSelectedRepositoriesEnabledGithubActionsOrganizationResponse;

    /**
     * PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/runners
     *
     * The self-hosted runner groups REST API is available with GitHub Enterprise Cloud. For more
     * information, see "[GitHub's products](https://docs.github.
     * com/github/getting-started-with-github/githubs-products)."
     * Replaces the list of self-hosted runners that are part of an organization runner group.
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     */
    abstract public function actionsSetSelfHostedRunnersInGroupForOrg(
        ActionsSetSelfHostedRunnersInGroupForOrgRequest $request,
        string $org,
        int $runner_group_id,
    ): ActionsSetSelfHostedRunnersInGroupForOrgResponse;

    /**
     * PATCH /orgs/{org}/actions/runner-groups/{runner_group_id}
     *
     * The self-hosted runner groups REST API is available with GitHub Enterprise Cloud. For more
     * information, see "[GitHub's products](https://docs.github.
     * com/github/getting-started-with-github/githubs-products)."
     * Updates the `name` and `visibility` of a self-hosted runner group in an organization.
     * You must authenticate using an access token with the `admin:org` scope to use this endpoint.
     */
    abstract public function actionsUpdateSelfHostedRunnerGroupForOrg(
        ActionsUpdateSelfHostedRunnerGroupForOrgRequest $request,
        string $org,
        int $runner_group_id,
    ): ActionsUpdateSelfHostedRunnerGroupForOrgResponse;

    /**
     * GET /orgs/{org}/events
     *
     * List public organization events
     */
    abstract public function activityListPublicOrgEvents(
        ActivityListPublicOrgEventsRequest $request,
        string $org,
    ): ActivityListPublicOrgEventsResponse;

    /**
     * GET /orgs/{org}/settings/billing/actions
     *
     * Gets the summary of the free and paid GitHub Actions minutes used.
     * Paid minutes only apply to workflows in private repositories that use GitHub-hosted runners.
     * Minutes used is listed for each GitHub-hosted runner operating system. Any job re-runs are also
     * included in the usage. The usage returned includes any minute multipliers for macOS and Windows
     * runners, and is rounded up to the nearest whole minute. For more information, see "[Managing
     * billing for GitHub Actions](https://help.github.
     * com/github/setting-up-and-managing-billing-and-payments-on-github/managing-billing-for-github-actions)".
     * Access tokens must have the `repo` or `admin:org` scope.
     */
    abstract public function billingGetGithubActionsBillingOrg(
        string $org,
    ): BillingGetGithubActionsBillingOrgResponse;

    /**
     * GET /orgs/{org}/settings/billing/packages
     *
     * Gets the free and paid storage used for GitHub Packages in gigabytes.
     * Paid minutes only apply to packages stored for private repositories. For more information, see
     * "[Managing billing for GitHub Packages](https://help.github.
     * com/github/setting-up-and-managing-billing-and-payments-on-github/managing-billing-for-github-packages)."
     * Access tokens must have the `repo` or `admin:org` scope.
     */
    abstract public function billingGetGithubPackagesBillingOrg(
        string $org,
    ): BillingGetGithubPackagesBillingOrgResponse;

    /**
     * GET /orgs/{org}/settings/billing/shared-storage
     *
     * Gets the estimated paid and estimated total storage used for GitHub Actions and Github Packages.
     * Paid minutes only apply to packages stored for private repositories. For more information, see
     * "[Managing billing for GitHub Packages](https://help.github.
     * com/github/setting-up-and-managing-billing-and-payments-on-github/managing-billing-for-github-packages)."
     * Access tokens must have the `repo` or `admin:org` scope.
     */
    abstract public function billingGetSharedStorageBillingOrg(
        string $org,
    ): BillingGetSharedStorageBillingOrgResponse;

    /**
     * DELETE /orgs/{org}/interaction-limits
     *
     * Removes all interaction restrictions from public repositories in the given organization. You must
     * be an organization owner to remove restrictions.
     */
    abstract public function interactionsRemoveRestrictionsForOrg(
        string $org,
    ): InteractionsRemoveRestrictionsForOrgResponse;

    /**
     * PUT /orgs/{org}/interaction-limits
     *
     * Temporarily restricts interactions to a certain type of GitHub user in any public repository in
     * the given organization. You must be an organization owner to set these restrictions. Setting the
     * interaction limit at the organization level will overwrite any interaction limits that are set for
     * individual repositories owned by the organization.
     */
    abstract public function interactionsSetRestrictionsForOrg(
        InteractionsSetRestrictionsForOrgRequest $request,
        string $org,
    ): InteractionsSetRestrictionsForOrgResponse;

    /**
     * GET /orgs/{org}/issues
     *
     * List issues in an organization assigned to the authenticated user.
     * **Note**: GitHub's REST API v3 considers every pull request an issue, but not every issue is a
     * pull request. For this
     * reason, "Issues" endpoints may return both issues and pull requests in the response. You can
     * identify pull requests by
     * the `pull_request` key. Be aware that the `id` of a pull request returned from "Issues" endpoints
     * will be an _issue id_. To find out the pull
     * request id, use the "[List pull requests](https://docs.github.
     * com/rest/reference/pulls#list-pull-requests)" endpoint.
     */
    abstract public function issuesListForOrg(
        IssuesListForOrgRequest $request,
        string $org,
    ): IssuesListForOrgResponse;

    /**
     * DELETE /orgs/{org}/migrations/{migration_id}/archive
     *
     * Deletes a previous migration archive. Migration archives are automatically deleted after seven
     * days.
     */
    abstract public function migrationsDeleteArchiveForOrg(
        string $org,
        int $migration_id,
    ): MigrationsDeleteArchiveForOrgResponse;

    /**
     * GET /orgs/{org}/migrations/{migration_id}/archive
     *
     * Fetches the URL to a migration archive.
     */
    abstract public function migrationsDownloadArchiveForOrg(
        string $org,
        int $migration_id,
    ): void;

    /**
     * GET /orgs/{org}/migrations/{migration_id}
     *
     * Fetches the status of a migration.
     * The `state` of a migration can be one of the following values:
     * *   `pending`, which means the migration hasn't started yet.
     * *   `exporting`, which means the migration is in progress.
     * *   `exported`, which means the migration finished successfully.
     * *   `failed`, which means the migration failed.
     */
    abstract public function migrationsGetStatusForOrg(
        MigrationsGetStatusForOrgRequest $request,
        string $org,
        int $migration_id,
    ): MigrationsGetStatusForOrgResponse;

    /**
     * GET /orgs/{org}/migrations
     *
     * Lists the most recent migrations.
     */
    abstract public function migrationsListForOrg(
        MigrationsListForOrgRequest $request,
        string $org,
    ): MigrationsListForOrgResponse;

    /**
     * GET /orgs/{org}/migrations/{migration_id}/repositories
     *
     * List all the repositories for this organization migration.
     */
    abstract public function migrationsListReposForOrg(
        MigrationsListReposForOrgRequest $request,
        string $org,
        int $migration_id,
    ): MigrationsListReposForOrgResponse;

    /**
     * POST /orgs/{org}/migrations
     *
     * Initiates the generation of a migration archive.
     */
    abstract public function migrationsStartForOrg(
        MigrationsStartForOrgRequest $request,
        string $org,
    ): MigrationsStartForOrgResponse;

    /**
     * DELETE /orgs/{org}/migrations/{migration_id}/repos/{repo_name}/lock
     *
     * Unlocks a repository that was locked for migration. You should unlock each migrated repository and
     * [delete them](https://docs.github.com/rest/reference/repos#delete-a-repository) when the migration
     * is complete and you no longer need the source data.
     */
    abstract public function migrationsUnlockRepoForOrg(
        string $org,
        int $migration_id,
        string $repo_name,
    ): MigrationsUnlockRepoForOrgResponse;

    /**
     * PUT /orgs/{org}/blocks/{username}
     *
     * Block a user from an organization
     */
    abstract public function orgsBlockUser(
        string $org,
        string $username,
    ): OrgsBlockUserResponse;

    /**
     * DELETE /orgs/{org}/invitations/{invitation_id}
     *
     * Cancel an organization invitation. In order to cancel an organization invitation, the
     * authenticated user must be an organization owner.
     * This endpoint triggers [notifications](https://docs.github.
     * com/en/github/managing-subscriptions-and-notifications-on-github/about-notifications).
     */
    abstract public function orgsCancelInvitation(
        string $org,
        int $invitation_id,
    ): OrgsCancelInvitationResponse;

    /**
     * GET /orgs/{org}/blocks/{username}
     *
     * Check if a user is blocked by an organization
     */
    abstract public function orgsCheckBlockedUser(
        string $org,
        string $username,
    ): OrgsCheckBlockedUserResponse;

    /**
     * GET /orgs/{org}/members/{username}
     *
     * Check if a user is, publicly or privately, a member of the organization.
     */
    abstract public function orgsCheckMembershipForUser(
        string $org,
        string $username,
    ): OrgsCheckMembershipForUserResponse;

    /**
     * GET /orgs/{org}/public_members/{username}
     *
     * Check public organization membership for a user
     */
    abstract public function orgsCheckPublicMembershipForUser(
        string $org,
        string $username,
    ): OrgsCheckPublicMembershipForUserResponse;

    /**
     * PUT /orgs/{org}/outside_collaborators/{username}
     *
     * When an organization member is converted to an outside collaborator, they'll only have access to
     * the repositories that their current team membership allows. The user will no longer be a member of
     * the organization. For more information, see "[Converting an organization member to an outside
     * collaborator](https://help.github.
     * com/articles/converting-an-organization-member-to-an-outside-collaborator/)".
     */
    abstract public function orgsConvertMemberToOutsideCollaborator(
        string $org,
        string $username,
    ): OrgsConvertMemberToOutsideCollaboratorResponse;

    /**
     * POST /orgs/{org}/invitations
     *
     * Invite people to an organization by using their GitHub user ID or their email address. In order to
     * create invitations in an organization, the authenticated user must be an organization owner.
     * This endpoint triggers [notifications](https://docs.github.
     * com/en/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating
     * content too quickly using this endpoint may result in secondary rate limiting. See "[Secondary
     * rate limits](https://docs.github.
     * com/rest/overview/resources-in-the-rest-api#secondary-rate-limits)" and "[Dealing with secondary
     * rate limits](https://docs.github.
     * com/rest/guides/best-practices-for-integrators#dealing-with-secondary-rate-limits)" for details.
     */
    abstract public function orgsCreateInvitation(
        OrgsCreateInvitationRequest $request,
        string $org,
    ): OrgsCreateInvitationResponse;

    /**
     * POST /orgs/{org}/hooks
     *
     * Here's how you can create a hook that posts payloads in JSON format:.
     */
    abstract public function orgsCreateWebhook(
        OrgsCreateWebhookRequest $request,
        string $org,
    ): OrgsCreateWebhookResponse;

    /**
     * DELETE /orgs/{org}/hooks/{hook_id}
     *
     * Delete an organization webhook
     */
    abstract public function orgsDeleteWebhook(
        string $org,
        int $hook_id,
    ): OrgsDeleteWebhookResponse;

    /**
     * GET /orgs/{org}
     *
     * To see many of the organization response values, you need to be an authenticated organization
     * owner with the `admin:org` scope. When the value of `two_factor_requirement_enabled` is `true`,
     * the organization requires all members, billing managers, and outside collaborators to enable
     * [two-factor authentication](https://help.github.
     * com/articles/securing-your-account-with-two-factor-authentication-2fa/).
     * GitHub Apps with the `Organization plan` permission can use this endpoint to retrieve information
     * about an organization's GitHub plan. See "[Authenticating with GitHub Apps](https://docs.github.
     * com/apps/building-github-apps/authenticating-with-github-apps/)" for details. For an example
     * response, see 'Response with GitHub plan information' below.".
     */
    abstract public function orgsGet(
        string $org,
    ): OrgsGetResponse;

    /**
     * GET /orgs/{org}/audit-log
     *
     * Gets the audit log for an organization. For more information, see "[Reviewing the audit log for
     * your organization](https://docs.github.
     * com/github/setting-up-and-managing-organizations-and-teams/reviewing-the-audit-log-for-your-organization)."
     * To use this endpoint, you must be an organization owner, and you must use an access token with the
     * `admin:org` scope. GitHub Apps must have the `organization_administration` read permission to use
     * this endpoint.
     */
    abstract public function orgsGetAuditLog(
        OrgsGetAuditLogRequest $request,
        string $org,
    ): OrgsGetAuditLogResponse;

    /**
     * GET /orgs/{org}/memberships/{username}
     *
     * In order to get a user's membership with an organization, the authenticated user must be an
     * organization member. The `state` parameter in the response can be used to identify the user's
     * membership status.
     */
    abstract public function orgsGetMembershipForUser(
        string $org,
        string $username,
    ): OrgsGetMembershipForUserResponse;

    /**
     * GET /orgs/{org}/hooks/{hook_id}
     *
     * Returns a webhook configured in an organization. To get only the webhook `config` properties, see
     * "[Get a webhook configuration for an
     * organization](/rest/reference/orgs#get-a-webhook-configuration-for-an-organization).".
     */
    abstract public function orgsGetWebhook(
        string $org,
        int $hook_id,
    ): OrgsGetWebhookResponse;

    /**
     * GET /orgs/{org}/hooks/{hook_id}/config
     *
     * Returns the webhook configuration for an organization. To get more information about the webhook,
     * including the `active` state and `events`, use "[Get an organization webhook
     * ](/rest/reference/orgs#get-an-organization-webhook)."
     * Access tokens must have the `admin:org_hook` scope, and GitHub Apps must have the
     * `organization_hooks:read` permission.
     */
    abstract public function orgsGetWebhookConfigForOrg(
        string $org,
        int $hook_id,
    ): OrgsGetWebhookConfigForOrgResponse;

    /**
     * GET /orgs/{org}/hooks/{hook_id}/deliveries/{delivery_id}
     *
     * Returns a delivery for a webhook configured in an organization.
     */
    abstract public function orgsGetWebhookDelivery(
        string $org,
        int $hook_id,
        int $delivery_id,
    ): OrgsGetWebhookDeliveryResponse;

    /**
     * GET /orgs/{org}/blocks
     *
     * List the users blocked by an organization.
     */
    abstract public function orgsListBlockedUsers(
        string $org,
    ): OrgsListBlockedUsersResponse;

    /**
     * GET /orgs/{org}/failed_invitations
     *
     * The return hash contains `failed_at` and `failed_reason` fields which represent the time at which
     * the invitation failed and the reason for the failure.
     */
    abstract public function orgsListFailedInvitations(
        OrgsListFailedInvitationsRequest $request,
        string $org,
    ): OrgsListFailedInvitationsResponse;

    /**
     * GET /orgs/{org}/invitations/{invitation_id}/teams
     *
     * List all teams associated with an invitation. In order to see invitations in an organization, the
     * authenticated user must be an organization owner.
     */
    abstract public function orgsListInvitationTeams(
        OrgsListInvitationTeamsRequest $request,
        string $org,
        int $invitation_id,
    ): OrgsListInvitationTeamsResponse;

    /**
     * GET /orgs/{org}/members
     *
     * List all users who are members of an organization. If the authenticated user is also a member of
     * this organization then both concealed and public members will be returned.
     */
    abstract public function orgsListMembers(
        OrgsListMembersRequest $request,
        string $org,
    ): OrgsListMembersResponse;

    /**
     * GET /orgs/{org}/outside_collaborators
     *
     * List all users who are outside collaborators of an organization.
     */
    abstract public function orgsListOutsideCollaborators(
        OrgsListOutsideCollaboratorsRequest $request,
        string $org,
    ): OrgsListOutsideCollaboratorsResponse;

    /**
     * GET /orgs/{org}/invitations
     *
     * The return hash contains a `role` field which refers to the Organization Invitation role and will
     * be one of the following values: `direct_member`, `admin`, `billing_manager`, `hiring_manager`, or
     * `reinstate`. If the invitee is not a GitHub member, the `login` field in the return hash will be
     * `null`.
     */
    abstract public function orgsListPendingInvitations(
        OrgsListPendingInvitationsRequest $request,
        string $org,
    ): OrgsListPendingInvitationsResponse;

    /**
     * GET /orgs/{org}/public_members
     *
     * Members of an organization can choose to have their membership publicized or not.
     */
    abstract public function orgsListPublicMembers(
        OrgsListPublicMembersRequest $request,
        string $org,
    ): OrgsListPublicMembersResponse;

    /**
     * GET /orgs/{org}/credential-authorizations
     *
     * Listing and deleting credential authorizations is available to organizations with GitHub
     * Enterprise Cloud. For more information, see [GitHub's products](https://help.github.
     * com/github/getting-started-with-github/githubs-products).
     * An authenticated organization owner with the `read:org` scope can list all credential
     * authorizations for an organization that uses SAML single sign-on (SSO). The credentials are either
     * personal access tokens or SSH keys that organization members have authorized for the organization.
     * For more information, see [About authentication with SAML single sign-on](https://help.github.
     * com/en/articles/about-authentication-with-saml-single-sign-on).
     */
    abstract public function orgsListSamlSsoAuthorizations(
        string $org,
    ): OrgsListSamlSsoAuthorizationsResponse;

    /**
     * GET /orgs/{org}/hooks/{hook_id}/deliveries
     *
     * Returns a list of webhook deliveries for a webhook configured in an organization.
     */
    abstract public function orgsListWebhookDeliveries(
        OrgsListWebhookDeliveriesRequest $request,
        string $org,
        int $hook_id,
    ): OrgsListWebhookDeliveriesResponse;

    /**
     * GET /orgs/{org}/hooks
     *
     * List organization webhooks
     */
    abstract public function orgsListWebhooks(
        OrgsListWebhooksRequest $request,
        string $org,
    ): OrgsListWebhooksResponse;

    /**
     * POST /orgs/{org}/hooks/{hook_id}/pings
     *
     * This will trigger a [ping event](https://docs.github.com/webhooks/#ping-event) to be sent to the
     * hook.
     */
    abstract public function orgsPingWebhook(
        string $org,
        int $hook_id,
    ): OrgsPingWebhookResponse;

    /**
     * POST /orgs/{org}/hooks/{hook_id}/deliveries/{delivery_id}/attempts
     *
     * Redeliver a delivery for a webhook configured in an organization.
     */
    abstract public function orgsRedeliverWebhookDelivery(
        string $org,
        int $hook_id,
        int $delivery_id,
    ): OrgsRedeliverWebhookDeliveryResponse;

    /**
     * DELETE /orgs/{org}/members/{username}
     *
     * Removing a user from this list will remove them from all teams and they will no longer have any
     * access to the organization's repositories.
     */
    abstract public function orgsRemoveMember(
        string $org,
        string $username,
    ): OrgsRemoveMemberResponse;

    /**
     * DELETE /orgs/{org}/memberships/{username}
     *
     * In order to remove a user's membership with an organization, the authenticated user must be an
     * organization owner.
     * If the specified user is an active member of the organization, this will remove them from the
     * organization. If the specified user has been invited to the organization, this will cancel their
     * invitation. The specified user will receive an email notification in both cases.
     */
    abstract public function orgsRemoveMembershipForUser(
        string $org,
        string $username,
    ): OrgsRemoveMembershipForUserResponse;

    /**
     * DELETE /orgs/{org}/outside_collaborators/{username}
     *
     * Removing a user from this list will remove them from all the organization's repositories.
     */
    abstract public function orgsRemoveOutsideCollaborator(
        string $org,
        string $username,
    ): OrgsRemoveOutsideCollaboratorResponse;

    /**
     * DELETE /orgs/{org}/public_members/{username}
     *
     * Remove public organization membership for the authenticated user
     */
    abstract public function orgsRemovePublicMembershipForAuthenticatedUser(
        string $org,
        string $username,
    ): OrgsRemovePublicMembershipForAuthenticatedUserResponse;

    /**
     * DELETE /orgs/{org}/credential-authorizations/{credential_id}
     *
     * Listing and deleting credential authorizations is available to organizations with GitHub
     * Enterprise Cloud. For more information, see [GitHub's products](https://help.github.
     * com/github/getting-started-with-github/githubs-products).
     * An authenticated organization owner with the `admin:org` scope can remove a credential
     * authorization for an organization that uses SAML SSO. Once you remove someone's credential
     * authorization, they will need to create a new personal access token or SSH key and authorize it
     * for the organization they want to access.
     */
    abstract public function orgsRemoveSamlSsoAuthorization(
        string $org,
        int $credential_id,
    ): OrgsRemoveSamlSsoAuthorizationResponse;

    /**
     * PUT /orgs/{org}/memberships/{username}
     *
     * Only authenticated organization owners can add a member to the organization or update the member's
     * role.
     * *   If the authenticated user is _adding_ a member to the organization, the invited user will
     * receive an email inviting them to the organization. The user's [membership status](https://docs.
     * github.com/rest/reference/orgs#get-organization-membership-for-a-user) will be `pending` until
     * they accept the invitation.
     * *   Authenticated users can _update_ a user's membership by passing the `role` parameter. If the
     * authenticated user changes a member's role to `admin`, the affected user will receive an email
     * notifying them that they've been made an organization owner. If the authenticated user changes an
     * owner's role to `member`, no email will be sent.
     * **Rate limits**
     * To prevent abuse, the authenticated user is limited to 50 organization invitations per 24 hour
     * period. If the organization is more than one month old or on a paid plan, the limit is 500
     * invitations per 24 hour period.
     */
    abstract public function orgsSetMembershipForUser(
        OrgsSetMembershipForUserRequest $request,
        string $org,
        string $username,
    ): OrgsSetMembershipForUserResponse;

    /**
     * PUT /orgs/{org}/public_members/{username}
     *
     * The user can publicize their own membership. (A user cannot publicize the membership for another
     * user.)
     * Note that you'll need to set `Content-Length` to zero when calling out to this endpoint. For more
     * information, see "[HTTP verbs](https://docs.github.
     * com/rest/overview/resources-in-the-rest-api#http-verbs).".
     */
    abstract public function orgsSetPublicMembershipForAuthenticatedUser(
        string $org,
        string $username,
    ): OrgsSetPublicMembershipForAuthenticatedUserResponse;

    /**
     * DELETE /orgs/{org}/blocks/{username}
     *
     * Unblock a user from an organization
     */
    abstract public function orgsUnblockUser(
        string $org,
        string $username,
    ): OrgsUnblockUserResponse;

    /**
     * PATCH /orgs/{org}/hooks/{hook_id}
     *
     * Updates a webhook configured in an organization. When you update a webhook, the `secret` will be
     * overwritten. If you previously had a `secret` set, you must provide the same `secret` or set a new
     * `secret` or the secret will be removed. If you are only updating individual webhook `config`
     * properties, use "[Update a webhook configuration for an
     * organization](/rest/reference/orgs#update-a-webhook-configuration-for-an-organization).".
     */
    abstract public function orgsUpdateWebhook(
        OrgsUpdateWebhookRequest $request,
        string $org,
        int $hook_id,
    ): OrgsUpdateWebhookResponse;

    /**
     * PATCH /orgs/{org}/hooks/{hook_id}/config
     *
     * Updates the webhook configuration for an organization. To update more information about the
     * webhook, including the `active` state and `events`, use "[Update an organization webhook
     * ](/rest/reference/orgs#update-an-organization-webhook)."
     * Access tokens must have the `admin:org_hook` scope, and GitHub Apps must have the
     * `organization_hooks:write` permission.
     */
    abstract public function orgsUpdateWebhookConfigForOrg(
        OrgsUpdateWebhookConfigForOrgRequest $request,
        string $org,
        int $hook_id,
    ): OrgsUpdateWebhookConfigForOrgResponse;

    /**
     * DELETE /orgs/{org}/packages/{package_type}/{package_name}
     *
     * Deletes an entire package in an organization. You cannot delete a public package if any version of
     * the package has more than 5,000 downloads. In this scenario, contact GitHub support for further
     * assistance.
     * To use this endpoint, you must have admin permissions in the organization and authenticate using
     * an access token with the `packages:read` and `packages:delete` scopes. In addition:
     * - If `package_type` is not `container`, your token must also include the `repo` scope.
     * - If `package_type` is `container`, you must also have admin permissions to the container you want
     * to delete.
     */
    abstract public function packagesDeletePackageForOrg(
        PackageType $package_type,
        string $package_name,
        string $org,
    ): PackagesDeletePackageForOrgResponse;

    /**
     * DELETE /orgs/{org}/packages/{package_type}/{package_name}/versions/{package_version_id}
     *
     * Deletes a specific package version in an organization. If the package is public and the package
     * version has more than 5,000 downloads, you cannot delete the package version. In this scenario,
     * contact GitHub support for further assistance.
     * To use this endpoint, you must have admin permissions in the organization and authenticate using
     * an access token with the `packages:read` and `packages:delete` scopes. In addition:
     * - If `package_type` is not `container`, your token must also include the `repo` scope.
     * - If `package_type` is `container`, you must also have admin permissions to the container you want
     * to delete.
     */
    abstract public function packagesDeletePackageVersionForOrg(
        PackageType $package_type,
        string $package_name,
        string $org,
        int $package_version_id,
    ): PackagesDeletePackageVersionForOrgResponse;

    /**
     * GET /orgs/{org}/packages/{package_type}/{package_name}/versions
     *
     * Returns all package versions for a package owned by an organization.
     * To use this endpoint, you must authenticate using an access token with the `packages:read` scope.
     * If `package_type` is not `container`, your token must also include the `repo` scope.
     */
    abstract public function packagesGetAllPackageVersionsForPackageOwnedByOrg(
        PackagesGetAllPackageVersionsForPackageOwnedByOrgRequest $request,
        PackageType $package_type,
        string $package_name,
        string $org,
    ): PackagesGetAllPackageVersionsForPackageOwnedByOrgResponse;

    /**
     * GET /orgs/{org}/packages/{package_type}/{package_name}
     *
     * Gets a specific package in an organization.
     * To use this endpoint, you must authenticate using an access token with the `packages:read` scope.
     * If `package_type` is not `container`, your token must also include the `repo` scope.
     */
    abstract public function packagesGetPackageForOrganization(
        PackageType $package_type,
        string $package_name,
        string $org,
    ): PackagesGetPackageForOrganizationResponse;

    /**
     * GET /orgs/{org}/packages/{package_type}/{package_name}/versions/{package_version_id}
     *
     * Gets a specific package version in an organization.
     * You must authenticate using an access token with the `packages:read` scope.
     * If `package_type` is not `container`, your token must also include the `repo` scope.
     */
    abstract public function packagesGetPackageVersionForOrganization(
        PackageType $package_type,
        string $package_name,
        string $org,
        int $package_version_id,
    ): PackagesGetPackageVersionForOrganizationResponse;

    /**
     * GET /orgs/{org}/packages
     *
     * Lists all packages in an organization readable by the user.
     * To use this endpoint, you must authenticate using an access token with the `packages:read` scope.
     * If `package_type` is not `container`, your token must also include the `repo` scope.
     */
    abstract public function packagesListPackagesForOrganization(
        PackagesListPackagesForOrganizationRequest $request,
        string $org,
    ): PackagesListPackagesForOrganizationResponse;

    /**
     * POST /orgs/{org}/packages/{package_type}/{package_name}/restore
     *
     * Restores an entire package in an organization.
     * You can restore a deleted package under the following conditions:
     * - The package was deleted within the last 30 days.
     * - The same package namespace and version is still available and not reused for a new package. If
     * the same package namespace is not available, you will not be able to restore your package. In this
     * scenario, to restore the deleted package, you must delete the new package that uses the deleted
     * package's namespace first.
     * To use this endpoint, you must have admin permissions in the organization and authenticate using
     * an access token with the `packages:read` and `packages:write` scopes. In addition:
     * - If `package_type` is not `container`, your token must also include the `repo` scope.
     * - If `package_type` is `container`, you must also have admin permissions to the container that you
     * want to restore.
     */
    abstract public function packagesRestorePackageForOrg(
        PackagesRestorePackageForOrgRequest $request,
        PackageType $package_type,
        string $package_name,
        string $org,
    ): PackagesRestorePackageForOrgResponse;

    /**
     * POST /orgs/{org}/packages/{package_type}/{package_name}/versions/{package_version_id}/restore
     *
     * Restores a specific package version in an organization.
     * You can restore a deleted package under the following conditions:
     * - The package was deleted within the last 30 days.
     * - The same package namespace and version is still available and not reused for a new package. If
     * the same package namespace is not available, you will not be able to restore your package. In this
     * scenario, to restore the deleted package, you must delete the new package that uses the deleted
     * package's namespace first.
     * To use this endpoint, you must have admin permissions in the organization and authenticate using
     * an access token with the `packages:read` and `packages:write` scopes. In addition:
     * - If `package_type` is not `container`, your token must also include the `repo` scope.
     * - If `package_type` is `container`, you must also have admin permissions to the container that you
     * want to restore.
     */
    abstract public function packagesRestorePackageVersionForOrg(
        PackageType $package_type,
        string $package_name,
        string $org,
        int $package_version_id,
    ): PackagesRestorePackageVersionForOrgResponse;

    /**
     * POST /orgs/{org}/projects
     *
     * Creates an organization project board. Returns a `404 Not Found` status if projects are disabled
     * in the organization. If you do not have sufficient privileges to perform this action, a `401
     * Unauthorized` or `410 Gone` status is returned.
     */
    abstract public function projectsCreateForOrg(
        ProjectsCreateForOrgRequest $request,
        string $org,
    ): ProjectsCreateForOrgResponse;

    /**
     * GET /orgs/{org}/projects
     *
     * Lists the projects in an organization. Returns a `404 Not Found` status if projects are disabled
     * in the organization. If you do not have sufficient privileges to perform this action, a `401
     * Unauthorized` or `410 Gone` status is returned.
     */
    abstract public function projectsListForOrg(
        ProjectsListForOrgRequest $request,
        string $org,
    ): ProjectsListForOrgResponse;

    /**
     * POST /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments/{comment_number}/reactions
     *
     * Create a reaction to a [team discussion comment](https://docs.github.
     * com/rest/reference/teams#discussion-comments). OAuth access tokens require the `write:discussion`
     * [scope](https://docs.github.com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/). A
     * response with an HTTP `200` status means that you already added the reaction type to this team
     * discussion comment.
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `POST
     * /organizations/:org_id/team/:team_id/discussions/:discussion_number/comments/:comment_number/reactions`.
     */
    abstract public function reactionsCreateForTeamDiscussionCommentInOrg(
        ReactionsCreateForTeamDiscussionCommentInOrgRequest $request,
        string $org,
        string $team_slug,
        int $discussion_number,
        int $comment_number,
    ): ReactionsCreateForTeamDiscussionCommentInOrgResponse;

    /**
     * POST /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/reactions
     *
     * Create a reaction to a [team discussion](https://docs.github.com/rest/reference/teams#discussions).
     *  OAuth access tokens require the `write:discussion` [scope](https://docs.github.
     * com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/). A response with an HTTP `200`
     * status means that you already added the reaction type to this team discussion.
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `POST
     * /organizations/:org_id/team/:team_id/discussions/:discussion_number/reactions`.
     */
    abstract public function reactionsCreateForTeamDiscussionInOrg(
        ReactionsCreateForTeamDiscussionInOrgRequest $request,
        string $org,
        string $team_slug,
        int $discussion_number,
    ): ReactionsCreateForTeamDiscussionInOrgResponse;

    /**
     * DELETE /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/reactions/{reaction_id}
     *
     * **Note:** You can also specify a team or organization with `team_id` and `org_id` using the route
     * `DELETE
     * /organizations/:org_id/team/:team_id/discussions/:discussion_number/reactions/:reaction_id`.
     * Delete a reaction to a [team discussion](https://docs.github.com/rest/reference/teams#discussions).
     *  OAuth access tokens require the `write:discussion` [scope](https://docs.github.
     * com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     */
    abstract public function reactionsDeleteForTeamDiscussion(
        string $org,
        string $team_slug,
        int $discussion_number,
        int $reaction_id,
    ): ReactionsDeleteForTeamDiscussionResponse;

    /**
     * DELETE /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments/{comment_number}/reactions/{reaction_id}
     *
     * **Note:** You can also specify a team or organization with `team_id` and `org_id` using the route
     * `DELETE
     * /organizations/:org_id/team/:team_id/discussions/:discussion_number/comments/:comment_number/reactions/:reaction_id`.
     * Delete a reaction to a [team discussion comment](https://docs.github.
     * com/rest/reference/teams#discussion-comments). OAuth access tokens require the `write:discussion`
     * [scope](https://docs.github.com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     */
    abstract public function reactionsDeleteForTeamDiscussionComment(
        string $org,
        string $team_slug,
        int $discussion_number,
        int $comment_number,
        int $reaction_id,
    ): ReactionsDeleteForTeamDiscussionCommentResponse;

    /**
     * GET /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments/{comment_number}/reactions
     *
     * List the reactions to a [team discussion comment](https://docs.github.
     * com/rest/reference/teams#discussion-comments/). OAuth access tokens require the `read:discussion`
     * [scope](https://docs.github.com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `GET
     * /organizations/:org_id/team/:team_id/discussions/:discussion_number/comments/:comment_number/reactions`.
     */
    abstract public function reactionsListForTeamDiscussionCommentInOrg(
        ReactionsListForTeamDiscussionCommentInOrgRequest $request,
        string $org,
        string $team_slug,
        int $discussion_number,
        int $comment_number,
    ): ReactionsListForTeamDiscussionCommentInOrgResponse;

    /**
     * GET /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/reactions
     *
     * List the reactions to a [team discussion](https://docs.github.
     * com/rest/reference/teams#discussions). OAuth access tokens require the `read:discussion`
     * [scope](https://docs.github.com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `GET
     * /organizations/:org_id/team/:team_id/discussions/:discussion_number/reactions`.
     */
    abstract public function reactionsListForTeamDiscussionInOrg(
        ReactionsListForTeamDiscussionInOrgRequest $request,
        string $org,
        string $team_slug,
        int $discussion_number,
    ): ReactionsListForTeamDiscussionInOrgResponse;

    /**
     * POST /orgs/{org}/repos
     *
     * Creates a new repository in the specified organization. The authenticated user must be a member of
     * the organization.
     * **OAuth scope requirements**
     * When using [OAuth](https://docs.github.
     * com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/), authorizations must include:
     * *   `public_repo` scope or `repo` scope to create a public repository. Note: For GitHub AE, use
     * `repo` scope to create an internal repository.
     * *   `repo` scope to create a private repository.
     */
    abstract public function reposCreateInOrg(
        ReposCreateInOrgRequest $request,
        string $org,
    ): ReposCreateInOrgResponse;

    /**
     * GET /orgs/{org}/repos
     *
     * Lists repositories for the specified organization.
     */
    abstract public function reposListForOrg(
        ReposListForOrgRequest $request,
        string $org,
    ): ReposListForOrgResponse;

    /**
     * GET /orgs/{org}/secret-scanning/alerts
     *
     * Lists all secret scanning alerts for all eligible repositories in an organization, from newest to
     * oldest.
     * To use this endpoint, you must be an administrator for the repository or organization, and you
     * must use an access token with the `repo` scope or `security_events` scope.
     * GitHub Apps must have the `secret_scanning_alerts` read permission to use this endpoint.
     */
    abstract public function secretScanningListAlertsForOrg(
        SecretScanningListAlertsForOrgRequest $request,
        string $org,
    ): SecretScanningListAlertsForOrgResponse;

    /**
     * PUT /orgs/{org}/teams/{team_slug}/memberships/{username}
     *
     * Team synchronization is available for organizations using GitHub Enterprise Cloud. For more
     * information, see [GitHub's products](https://help.github.
     * com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
     * Adds an organization member to a team. An authenticated organization owner or team maintainer can
     * add organization members to a team.
     * **Note:** When you have team synchronization set up for a team with your organization's identity
     * provider (IdP), you will see an error if you attempt to use the API for making changes to the
     * team's membership. If you have access to manage group membership in your IdP, you can manage
     * GitHub team membership through your identity provider, which automatically adds and removes team
     * members in an organization. For more information, see "[Synchronizing teams between your identity
     * provider and GitHub](https://help.github.
     * com/articles/synchronizing-teams-between-your-identity-provider-and-github/)."
     * An organization owner can add someone who is not part of the team's organization to a team. When
     * an organization owner adds someone to a team who is not an organization member, this endpoint will
     * send an invitation to the person via email. This newly-created membership will be in the "pending"
     * state until the person accepts the invitation, at which point the membership will transition to
     * the "active" state and the user will be added as a member of the team.
     * If the user is already a member of the team, this endpoint will update the role of the team
     * member's role. To update the membership of a team member, the authenticated user must be an
     * organization owner or a team maintainer.
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `PUT
     * /organizations/{org_id}/team/{team_id}/memberships/{username}`.
     */
    abstract public function teamsAddOrUpdateMembershipForUserInOrg(
        TeamsAddOrUpdateMembershipForUserInOrgRequest $request,
        string $org,
        string $team_slug,
        string $username,
    ): TeamsAddOrUpdateMembershipForUserInOrgResponse;

    /**
     * PUT /orgs/{org}/teams/{team_slug}/projects/{project_id}
     *
     * Adds an organization project to a team. To add a project to a team or update the team's permission
     * on a project, the authenticated user must have `admin` permissions for the project. The project
     * and team must be part of the same organization.
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `PUT
     * /organizations/{org_id}/team/{team_id}/projects/{project_id}`.
     */
    abstract public function teamsAddOrUpdateProjectPermissionsInOrg(
        TeamsAddOrUpdateProjectPermissionsInOrgRequest $request,
        string $org,
        string $team_slug,
        int $project_id,
    ): TeamsAddOrUpdateProjectPermissionsInOrgResponse;

    /**
     * PUT /orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}
     *
     * To add a repository to a team or update the team's permission on a repository, the authenticated
     * user must have admin access to the repository, and must be able to see the team. The repository
     * must be owned by the organization, or a direct fork of a repository owned by the organization. You
     * will get a `422 Unprocessable Entity` status if you attempt to add a repository to a team that is
     * not owned by the organization. Note that, if you choose not to pass any parameters, you'll need to
     * set `Content-Length` to zero when calling out to this endpoint. For more information, see "[HTTP
     * verbs](https://docs.github.com/rest/overview/resources-in-the-rest-api#http-verbs)."
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `PUT
     * /organizations/{org_id}/team/{team_id}/repos/{owner}/{repo}`.
     * For more information about the permission levels, see "[Repository permission levels for an
     * organization](https://help.github.
     * com/en/github/setting-up-and-managing-organizations-and-teams/repository-permission-levels-for-an-organization#permission-levels-for-repositories-owned-by-an-organization)".
     */
    abstract public function teamsAddOrUpdateRepoPermissionsInOrg(
        TeamsAddOrUpdateRepoPermissionsInOrgRequest $request,
        string $org,
        string $team_slug,
        string $owner,
        string $repo,
    ): TeamsAddOrUpdateRepoPermissionsInOrgResponse;

    /**
     * GET /orgs/{org}/teams/{team_slug}/projects/{project_id}
     *
     * Checks whether a team has `read`, `write`, or `admin` permissions for an organization project. The
     * response includes projects inherited from a parent team.
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `GET
     * /organizations/{org_id}/team/{team_id}/projects/{project_id}`.
     */
    abstract public function teamsCheckPermissionsForProjectInOrg(
        string $org,
        string $team_slug,
        int $project_id,
    ): TeamsCheckPermissionsForProjectInOrgResponse;

    /**
     * GET /orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}
     *
     * Checks whether a team has `admin`, `push`, `maintain`, `triage`, or `pull` permission for a
     * repository. Repositories inherited through a parent team will also be checked.
     * You can also get information about the specified repository, including what permissions the team
     * grants on it, by passing the following custom [media type](https://docs.github.
     * com/rest/overview/media-types/) via the `application/vnd.github.v3.repository+json` accept header.
     * If a team doesn't have permission for the repository, you will receive a `404 Not Found` response
     * status.
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `GET
     * /organizations/{org_id}/team/{team_id}/repos/{owner}/{repo}`.
     */
    abstract public function teamsCheckPermissionsForRepoInOrg(
        string $org,
        string $team_slug,
        string $owner,
        string $repo,
    ): TeamsCheckPermissionsForRepoInOrgResponse;

    /**
     * POST /orgs/{org}/teams
     *
     * To create a team, the authenticated user must be a member or owner of `{org}`. By default,
     * organization members can create teams. Organization owners can limit team creation to organization
     * owners. For more information, see "[Setting team creation permissions](https://help.github.
     * com/en/articles/setting-team-creation-permissions-in-your-organization)."
     * When you create a new team, you automatically become a team maintainer without explicitly adding
     * yourself to the optional array of `maintainers`. For more information, see "[About
     * teams](https://help.github.
     * com/en/github/setting-up-and-managing-organizations-and-teams/about-teams)".
     */
    abstract public function teamsCreate(
        TeamsCreateRequest $request,
        string $org,
    ): TeamsCreateResponse;

    /**
     * POST /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments
     *
     * Creates a new comment on a team discussion. OAuth access tokens require the `write:discussion`
     * [scope](https://docs.github.com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     * This endpoint triggers [notifications](https://docs.github.
     * com/en/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating
     * content too quickly using this endpoint may result in secondary rate limiting. See "[Secondary
     * rate limits](https://docs.github.
     * com/rest/overview/resources-in-the-rest-api#secondary-rate-limits)" and "[Dealing with secondary
     * rate limits](https://docs.github.
     * com/rest/guides/best-practices-for-integrators#dealing-with-secondary-rate-limits)" for details.
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `POST
     * /organizations/{org_id}/team/{team_id}/discussions/{discussion_number}/comments`.
     */
    abstract public function teamsCreateDiscussionCommentInOrg(
        TeamsCreateDiscussionCommentInOrgRequest $request,
        string $org,
        string $team_slug,
        int $discussion_number,
    ): TeamsCreateDiscussionCommentInOrgResponse;

    /**
     * POST /orgs/{org}/teams/{team_slug}/discussions
     *
     * Creates a new discussion post on a team's page. OAuth access tokens require the `write:discussion`
     * [scope](https://docs.github.com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     * This endpoint triggers [notifications](https://docs.github.
     * com/en/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating
     * content too quickly using this endpoint may result in secondary rate limiting. See "[Secondary
     * rate limits](https://docs.github.
     * com/rest/overview/resources-in-the-rest-api#secondary-rate-limits)" and "[Dealing with secondary
     * rate limits](https://docs.github.
     * com/rest/guides/best-practices-for-integrators#dealing-with-secondary-rate-limits)" for details.
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `POST
     * /organizations/{org_id}/team/{team_id}/discussions`.
     */
    abstract public function teamsCreateDiscussionInOrg(
        TeamsCreateDiscussionInOrgRequest $request,
        string $org,
        string $team_slug,
    ): TeamsCreateDiscussionInOrgResponse;

    /**
     * PATCH /orgs/{org}/teams/{team_slug}/team-sync/group-mappings
     *
     * Team synchronization is available for organizations using GitHub Enterprise Cloud. For more
     * information, see [GitHub's products](https://help.github.
     * com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
     * Creates, updates, or removes a connection between a team and an IdP group. When adding groups to a
     * team, you must include all new and existing groups to avoid replacing existing groups with the new
     * ones. Specifying an empty `groups` array will remove all connections for a team.
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `PATCH
     * /organizations/{org_id}/team/{team_id}/team-sync/group-mappings`.
     */
    abstract public function teamsCreateOrUpdateIdpGroupConnectionsInOrg(
        TeamsCreateOrUpdateIdpGroupConnectionsInOrgRequest $request,
        string $org,
        string $team_slug,
    ): TeamsCreateOrUpdateIdpGroupConnectionsInOrgResponse;

    /**
     * DELETE /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments/{comment_number}
     *
     * Deletes a comment on a team discussion. OAuth access tokens require the `write:discussion`
     * [scope](https://docs.github.com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `DELETE
     * /organizations/{org_id}/team/{team_id}/discussions/{discussion_number}/comments/{comment_number}`.
     */
    abstract public function teamsDeleteDiscussionCommentInOrg(
        string $org,
        string $team_slug,
        int $discussion_number,
        int $comment_number,
    ): TeamsDeleteDiscussionCommentInOrgResponse;

    /**
     * DELETE /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}
     *
     * Delete a discussion from a team's page. OAuth access tokens require the `write:discussion`
     * [scope](https://docs.github.com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `DELETE
     * /organizations/{org_id}/team/{team_id}/discussions/{discussion_number}`.
     */
    abstract public function teamsDeleteDiscussionInOrg(
        string $org,
        string $team_slug,
        int $discussion_number,
    ): TeamsDeleteDiscussionInOrgResponse;

    /**
     * DELETE /orgs/{org}/teams/{team_slug}
     *
     * To delete a team, the authenticated user must be an organization owner or team maintainer.
     * If you are an organization owner, deleting a parent team will delete all of its child teams as
     * well.
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `DELETE
     * /organizations/{org_id}/team/{team_id}`.
     */
    abstract public function teamsDeleteInOrg(
        string $org,
        string $team_slug,
    ): TeamsDeleteInOrgResponse;

    /**
     * GET /orgs/{org}/teams/{team_slug}
     *
     * Gets a team using the team's `slug`. GitHub generates the `slug` from the team `name`.
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `GET
     * /organizations/{org_id}/team/{team_id}`.
     */
    abstract public function teamsGetByName(
        string $org,
        string $team_slug,
    ): TeamsGetByNameResponse;

    /**
     * GET /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments/{comment_number}
     *
     * Get a specific comment on a team discussion. OAuth access tokens require the `read:discussion`
     * [scope](https://docs.github.com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `GET
     * /organizations/{org_id}/team/{team_id}/discussions/{discussion_number}/comments/{comment_number}`.
     */
    abstract public function teamsGetDiscussionCommentInOrg(
        string $org,
        string $team_slug,
        int $discussion_number,
        int $comment_number,
    ): TeamsGetDiscussionCommentInOrgResponse;

    /**
     * GET /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}
     *
     * Get a specific discussion on a team's page. OAuth access tokens require the `read:discussion`
     * [scope](https://docs.github.com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `GET
     * /organizations/{org_id}/team/{team_id}/discussions/{discussion_number}`.
     */
    abstract public function teamsGetDiscussionInOrg(
        string $org,
        string $team_slug,
        int $discussion_number,
    ): TeamsGetDiscussionInOrgResponse;

    /**
     * GET /orgs/{org}/teams/{team_slug}/memberships/{username}
     *
     * Team members will include the members of child teams.
     * To get a user's membership with a team, the team must be visible to the authenticated user.
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `GET
     * /organizations/{org_id}/team/{team_id}/memberships/{username}`.
     * **Note:**
     * The response contains the `state` of the membership and the member's `role`.
     * The `role` for organization owners is set to `maintainer`. For more information about `maintainer`
     * roles, see see [Create a team](https://docs.github.com/rest/reference/teams#create-a-team).
     */
    abstract public function teamsGetMembershipForUserInOrg(
        string $org,
        string $team_slug,
        string $username,
    ): TeamsGetMembershipForUserInOrgResponse;

    /**
     * GET /orgs/{org}/teams
     *
     * Lists all teams in an organization that are visible to the authenticated user.
     */
    abstract public function teamsList(
        TeamsListRequest $request,
        string $org,
    ): TeamsListResponse;

    /**
     * GET /orgs/{org}/teams/{team_slug}/teams
     *
     * Lists the child teams of the team specified by `{team_slug}`.
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `GET
     * /organizations/{org_id}/team/{team_id}/teams`.
     */
    abstract public function teamsListChildInOrg(
        TeamsListChildInOrgRequest $request,
        string $org,
        string $team_slug,
    ): TeamsListChildInOrgResponse;

    /**
     * GET /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments
     *
     * List all comments on a team discussion. OAuth access tokens require the `read:discussion`
     * [scope](https://docs.github.com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `GET
     * /organizations/{org_id}/team/{team_id}/discussions/{discussion_number}/comments`.
     */
    abstract public function teamsListDiscussionCommentsInOrg(
        TeamsListDiscussionCommentsInOrgRequest $request,
        string $org,
        string $team_slug,
        int $discussion_number,
    ): TeamsListDiscussionCommentsInOrgResponse;

    /**
     * GET /orgs/{org}/teams/{team_slug}/discussions
     *
     * List all discussions on a team's page. OAuth access tokens require the `read:discussion`
     * [scope](https://docs.github.com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `GET
     * /organizations/{org_id}/team/{team_id}/discussions`.
     */
    abstract public function teamsListDiscussionsInOrg(
        TeamsListDiscussionsInOrgRequest $request,
        string $org,
        string $team_slug,
    ): TeamsListDiscussionsInOrgResponse;

    /**
     * GET /orgs/{org}/team-sync/groups
     *
     * Team synchronization is available for organizations using GitHub Enterprise Cloud. For more
     * information, see [GitHub's products](https://help.github.
     * com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
     * List IdP groups available in an organization. You can limit your page results using the `per_page`
     * parameter. GitHub generates a url-encoded `page` token using a cursor value for where the next
     * page begins. For more information on cursor pagination, see "[Offset and Cursor Pagination
     * explained](https://dev.to/jackmarchant/offset-and-cursor-pagination-explained-b89).".
     */
    abstract public function teamsListIdpGroupsForOrg(
        TeamsListIdpGroupsForOrgRequest $request,
        string $org,
    ): TeamsListIdpGroupsForOrgResponse;

    /**
     * GET /orgs/{org}/teams/{team_slug}/team-sync/group-mappings
     *
     * Team synchronization is available for organizations using GitHub Enterprise Cloud. For more
     * information, see [GitHub's products](https://help.github.
     * com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
     * List IdP groups connected to a team on GitHub.
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `GET
     * /organizations/{org_id}/team/{team_id}/team-sync/group-mappings`.
     */
    abstract public function teamsListIdpGroupsInOrg(
        string $org,
        string $team_slug,
    ): TeamsListIdpGroupsInOrgResponse;

    /**
     * GET /orgs/{org}/teams/{team_slug}/members
     *
     * Team members will include the members of child teams.
     * To list members in a team, the team must be visible to the authenticated user.
     */
    abstract public function teamsListMembersInOrg(
        TeamsListMembersInOrgRequest $request,
        string $org,
        string $team_slug,
    ): TeamsListMembersInOrgResponse;

    /**
     * GET /orgs/{org}/teams/{team_slug}/invitations
     *
     * The return hash contains a `role` field which refers to the Organization Invitation role and will
     * be one of the following values: `direct_member`, `admin`, `billing_manager`, `hiring_manager`, or
     * `reinstate`. If the invitee is not a GitHub member, the `login` field in the return hash will be
     * `null`.
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `GET
     * /organizations/{org_id}/team/{team_id}/invitations`.
     */
    abstract public function teamsListPendingInvitationsInOrg(
        TeamsListPendingInvitationsInOrgRequest $request,
        string $org,
        string $team_slug,
    ): TeamsListPendingInvitationsInOrgResponse;

    /**
     * GET /orgs/{org}/teams/{team_slug}/projects
     *
     * Lists the organization projects for a team.
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `GET
     * /organizations/{org_id}/team/{team_id}/projects`.
     */
    abstract public function teamsListProjectsInOrg(
        TeamsListProjectsInOrgRequest $request,
        string $org,
        string $team_slug,
    ): TeamsListProjectsInOrgResponse;

    /**
     * GET /orgs/{org}/teams/{team_slug}/repos
     *
     * Lists a team's repositories visible to the authenticated user.
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `GET
     * /organizations/{org_id}/team/{team_id}/repos`.
     */
    abstract public function teamsListReposInOrg(
        TeamsListReposInOrgRequest $request,
        string $org,
        string $team_slug,
    ): TeamsListReposInOrgResponse;

    /**
     * DELETE /orgs/{org}/teams/{team_slug}/memberships/{username}
     *
     * Team synchronization is available for organizations using GitHub Enterprise Cloud. For more
     * information, see [GitHub's products](https://help.github.
     * com/github/getting-started-with-github/githubs-products) in the GitHub Help documentation.
     * To remove a membership between a user and a team, the authenticated user must have 'admin'
     * permissions to the team or be an owner of the organization that the team is associated with.
     * Removing team membership does not delete the user, it just removes their membership from the team.
     * **Note:** When you have team synchronization set up for a team with your organization's identity
     * provider (IdP), you will see an error if you attempt to use the API for making changes to the
     * team's membership. If you have access to manage group membership in your IdP, you can manage
     * GitHub team membership through your identity provider, which automatically adds and removes team
     * members in an organization. For more information, see "[Synchronizing teams between your identity
     * provider and GitHub](https://help.github.
     * com/articles/synchronizing-teams-between-your-identity-provider-and-github/)."
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `DELETE
     * /organizations/{org_id}/team/{team_id}/memberships/{username}`.
     */
    abstract public function teamsRemoveMembershipForUserInOrg(
        string $org,
        string $team_slug,
        string $username,
    ): TeamsRemoveMembershipForUserInOrgResponse;

    /**
     * DELETE /orgs/{org}/teams/{team_slug}/projects/{project_id}
     *
     * Removes an organization project from a team. An organization owner or a team maintainer can remove
     * any project from the team. To remove a project from a team as an organization member, the
     * authenticated user must have `read` access to both the team and project, or `admin` access to the
     * team or project. This endpoint removes the project from the team, but does not delete the project.
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `DELETE
     * /organizations/{org_id}/team/{team_id}/projects/{project_id}`.
     */
    abstract public function teamsRemoveProjectInOrg(
        string $org,
        string $team_slug,
        int $project_id,
    ): TeamsRemoveProjectInOrgResponse;

    /**
     * DELETE /orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}
     *
     * If the authenticated user is an organization owner or a team maintainer, they can remove any
     * repositories from the team. To remove a repository from a team as an organization member, the
     * authenticated user must have admin access to the repository and must be able to see the team. This
     * does not delete the repository, it just removes it from the team.
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `DELETE
     * /organizations/{org_id}/team/{team_id}/repos/{owner}/{repo}`.
     */
    abstract public function teamsRemoveRepoInOrg(
        string $org,
        string $team_slug,
        string $owner,
        string $repo,
    ): TeamsRemoveRepoInOrgResponse;

    /**
     * PATCH /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments/{comment_number}
     *
     * Edits the body text of a discussion comment. OAuth access tokens require the `write:discussion`
     * [scope](https://docs.github.com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `PATCH
     * /organizations/{org_id}/team/{team_id}/discussions/{discussion_number}/comments/{comment_number}`.
     */
    abstract public function teamsUpdateDiscussionCommentInOrg(
        TeamsUpdateDiscussionCommentInOrgRequest $request,
        string $org,
        string $team_slug,
        int $discussion_number,
        int $comment_number,
    ): TeamsUpdateDiscussionCommentInOrgResponse;

    /**
     * PATCH /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}
     *
     * Edits the title and body text of a discussion post. Only the parameters you provide are updated.
     * OAuth access tokens require the `write:discussion` [scope](https://docs.github.
     * com/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `PATCH
     * /organizations/{org_id}/team/{team_id}/discussions/{discussion_number}`.
     */
    abstract public function teamsUpdateDiscussionInOrg(
        TeamsUpdateDiscussionInOrgRequest $request,
        string $org,
        string $team_slug,
        int $discussion_number,
    ): TeamsUpdateDiscussionInOrgResponse;

    /**
     * PATCH /orgs/{org}/teams/{team_slug}
     *
     * To edit a team, the authenticated user must either be an organization owner or a team maintainer.
     * **Note:** You can also specify a team by `org_id` and `team_id` using the route `PATCH
     * /organizations/{org_id}/team/{team_id}`.
     */
    abstract public function teamsUpdateInOrg(
        TeamsUpdateInOrgRequest $request,
        string $org,
        string $team_slug,
    ): TeamsUpdateInOrgResponse;
}
